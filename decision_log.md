# AIG Decision Log

_Per Phase 1 framework directive Part 7. Append-only; oldest at top, newest at bottom._
_Written by `scripts/decision_log_append.py`. Sync mirror: `decision_log.json`._

## Entries

### Withdraw Path 3 (post-hoc gate amendment) — Session 5 audit response

- **ts_utc:** `2026-05-21T08:00:00+00:00`
- **methodology_source:** `pre_framework_legacy`
- **decision:** Path 3 ('amend PORTFOLIO_GATE for small markets to lower trade-count + drop WR floor') REJECTED. Removed from Decision Point queue. UAE/Crypto FAILs accepted honestly under the original gate.
- **rationale:** Auditor Cowork flagged Path 3 as post-hoc threshold loosening — exactly the failure mode pre-registration discipline exists to prevent. Two acceptable alternatives logged: (a) accept FAILs as honest verdicts, (b) design a small-markets framework prospectively before seeing data.
- **alternatives_considered:**
  - Adopt Path 3 as written (rejected — data-mining via amendment)
  - Design small-markets framework prospectively on untouched data (deferred to Phase B / Phase 2)
- **audit_finding_refs:** `Session 5 Concern 2`
### Deploy US Divergence Daily to paper-forward

- **ts_utc:** `2026-05-21T09:50:00+00:00`
- **methodology_source:** `pre_framework_legacy`
- **decision:** scripts/paper_forward_divergence.py deployed with state file persistence, Telegram alerts on entry/exit. Pine TV-slot route deferred.
- **rationale:** US Divergence Daily passed PORTFOLIO_CLEARED under N=6 haircut (dSharpe 2.606, exp 1.227, WR 44.78%, 10,715 trades). Audit BLOCKING items resolved Session 5. Paper-forward stays paper-forward; no real money until ≥6 months forward data under the same gate.
- **alternatives_considered:**
  - Pine TV-slot deployment (deferred; Python-engine route preferred for now)
  - Defer paper-forward until UAE + Crypto also cleared (rejected; multi-strategy/multi-market not a deployment blocker)
- **audit_finding_refs:** `Session 5 Concern 1`, `Session 5 Concern 5`
### Watch list = top-5 by per-ticker OOS expectancy (SUPERSEDED)

- **ts_utc:** `2026-05-21T09:55:00+00:00`
- **methodology_source:** `pre_framework_legacy`
- **decision:** Initial paper-forward watch list set to DY, EXPGY, PSX, ARW, ROL — the five tickers with the highest per-ticker OOS expectancy in the cleared US Divergence run. SUPERSEDED 2026-05-21 by random-sample N=50 seed=42 stratification; further SUPERSEDED 2026-05-22 by Phase 1 directive Part 2 Improvement 1 (full cleared universe OR stratified sample).
- **rationale:** Original choice was operational — small list for attention budget. Audit NEW-1 BLOCKING flagged this as cherry-picking — high per-ticker expectancy = high sampling noise, not high forward edge.
- **alternatives_considered:**
  - Full cleared universe (Phase 1 Improvement 1 Option A — adopted later)
  - Stratified sample (Phase 1 Improvement 1 Option B — adopted later)
- **audit_finding_refs:** `Session 5 NEW-1`
### Expand UAE universe 44 → 64 retrievable tickers

- **ts_utc:** `2026-05-21T10:18:00+00:00`
- **methodology_source:** `pre_framework_legacy`
- **decision:** Added 20 UAE tickers via TV-MCP cache: ADAVIATION, ADSB, ADNH, BILDCO, others. Universe file universe/uae_tickers_full.txt grew to 64.
- **rationale:** Phase 1 Item 3 required ≥60 retrievable UAE tickers. Pre-existing 44 cached + 20 sourced via TV-MCP. Validation on 60-ticker subset already ran (45 valid, 54 trades, PORTFOLIO_FAIL — expected small-market verdict).
- **alternatives_considered:**
  - Accept 44-ticker universe (rejected — below 60-floor)
  - Source from a non-TV-MCP feed (rejected — TV-MCP is the only halal-verified UAE source we have wired)
### Accept NO CERTIFIABLE CRYPTO EDGE verdict

- **ts_utc:** `2026-05-21T11:35:00+00:00`
- **methodology_source:** `pre_framework_legacy`
- **decision:** Crypto Divergence 1D portfolio result (140 valid, 107 contrib, 644 trades, exp 3.54, WR 37.1%, est dSharpe 0.05) fails multi-criterion (WR floor, trade count, Sharpe). No certifiable crypto edge under current strategies. Crypto FAIL accepted honestly.
- **rationale:** Per audit Concern 2 + Concern 4: WR floor binding as deployability constraint; crypto's 37.1% WR is the primary blocker; adding bars / TF expansion = post-hoc loosening on a failing dataset. Future crypto edge requires a small-markets framework frozen before seeing data.
- **alternatives_considered:**
  - Iterate to 4H or confluence (rejected — same-strategy iteration on failing dataset, Concern 2)
  - Lower WR floor for crypto only (rejected — post-hoc threshold change, Concern 2)
- **audit_finding_refs:** `Session 5 Concern 2`, `Session 5 Concern 4`
### CC crash auto-recovery — watchdog design choices

- **ts_utc:** `2026-05-22T09:35:00+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Built scripts/cc_watchdog.ps1 with sentinel-stale-OR-process-absent detection (not process-presence-only), headless `claude -p` recovery (not SendKeys to Windows Terminal), and sacrificial-subprocess test (not killing the live session). Windows Scheduled Task AIG-CC-Watchdog runs every 60s in user INTERACTIVE session.
- **rationale:** Process-presence-only detection would have missed the 2026-05-21→22 overnight 29-fire-miss incident (the process stayed ALIVE while the REPL hung). SendKeys is fragile under locked-screen / focus-shift conditions; headless claude -p is deterministic. Sacrificial subprocess test preserves the build session.
- **alternatives_considered:**
  - Process-presence only + SendKeys recovery (per original spec — rejected as fragile)
  - Kill claude.exe to test (rejected — would terminate the build session)
### Receipt of Phase 1 framework directive (ahmed_response_2026-05-22)

- **ts_utc:** `2026-05-22T15:55:00+00:00`
- **methodology_source:** `framework_directive`
- **decision:** Ahmed delivered comprehensive Phase 1 framework directive: 6 gate amendments, 6 watch-list improvements, re-validation discipline, three-filter selection methodology, Phase 1 strategy cap=4, framework finality (6-month freeze from commit date).
- **rationale:** Replaces ad-hoc 'what should we try' with frozen executable methodology. Closes audit NEW-1 (watch list), NEW-3 (Decision Log), NEW-5 (Telegram log). Prevents amendment-chasing if Phase 1 results disappoint by binding the framework for 6 months.
- **audit_finding_refs:** `Session 5 NEW-1`, `Session 5 NEW-3`, `Session 5 NEW-5`
### Phase 1 cap = 4 candidates; Divergence grandfathered slot 1

- **ts_utc:** `2026-05-22T15:58:00+00:00`
- **methodology_source:** `ahmed_override`
- **decision:** Phase 1 tests a maximum of 4 candidate strategies total under the three-filter methodology. Divergence fills slot 1 by grandfathering (Session-5 framework selection, pre-autonomous-loop). 3 slots remain. After 4 complete, Phase 1 testing closes until Phase 2 review post-6-month freeze.
- **rationale:** Cap prevents 'keep testing until something clears' from becoming the de facto methodology. Grandfathering Divergence preserves the legitimately-cleared US strategy without inflating the trial budget retroactively.
- **alternatives_considered:**
  - Cap fills with 4 cleared strategies (Divergence+MBV+PMR+STR) — Phase 1 closes immediately (rejected per Ahmed)
  - All 13 strategies count as Phase 1 — directive applies only to Phase 2 (rejected — would defer the new methodology indefinitely)
### 13 sprint-loop strategies tagged Pre-Framework, sprint-loop-tested

- **ts_utc:** `2026-05-22T15:58:30+00:00`
- **methodology_source:** `ahmed_override`
- **decision:** EMA-200, MBV, DBO, ROC, VCB, HAT, PMR, STR, ART, CMF, GAP, WCK + Divergence (grandfathered) get a Pre-Framework field in strategy_register.md. Verdicts stand as historical record. n_trials_registered=39 stays binding (cannot selectively reduce N without invalidating the discipline that produced the count).
- **rationale:** Wipe-slate per Q1=B-modified. Existing test data is reusable as initial evidence for the three-filter methodology, but any Pre-Framework strategy selected by the methodology MUST re-validate under the amended gate on data not previously evaluated under those amendments.
- **alternatives_considered:**
  - Cap fills with 4 cleared (option A — rejected, would close Phase 1 immediately and bypass the new methodology)
  - Phase 1 already complete (option C — rejected, would skip Phase 1 entirely)
### Pause autonomous sprint cron (aig-mode1-sprint)

- **ts_utc:** `2026-05-22T16:00:00+00:00`
- **methodology_source:** `framework_directive`
- **decision:** Disabled Cloud Routine `aig-mode1-sprint`. Watchdog (AIG-CC-Watchdog Windows Scheduled Task) stays running. Paper-forward detector + dashboard regen + commit don't fire automatically until SKILL.md is rewritten to directive-compliant flow.
- **rationale:** The cron was running the Version-B autonomous methodology (enumerate prior primitives → pick unused domain), which Part 4 of the directive explicitly forbids. SKILL.md must be rewritten before the cron can re-enable.
- **alternatives_considered:**
  - Keep cron firing with strategy-enrollment step removed (rejected — risk of stale state piling up, simpler to pause cleanly)
  - Let cron run until WCK finalizes (rejected — risk of Strategy 14 enrolling under old methodology in the window)
### WCK US drain to completion as Pre-Framework

- **ts_utc:** `2026-05-22T16:01:00+00:00`
- **methodology_source:** `ahmed_override`
- **decision:** WCK US (mid-drain at 800/1603 = 50%) is allowed to finish via manual `staged_validate --step` calls. Verdict landed as historical record. Tagged Pre-Framework regardless of outcome — not a Phase 1 candidate.
- **rationale:** Stopping mid-drain creates an INCOMPLETE_SWEEP entry that violates the sweep rule. Finishing honors the discipline that already produced 800 ticker results. Outcome is honest history, not framework data.
- **alternatives_considered:**
  - Stop now, mark INCOMPLETE_SWEEP (rejected — sweep-rule violation per Phase 1 directive)
### Decision Log = dual format (.md + .json) with methodology_source

- **ts_utc:** `2026-05-22T16:01:30+00:00`
- **methodology_source:** `ahmed_override`
- **decision:** decision_log.md (human-readable, append-only Markdown) + decision_log.json (machine-readable mirror, dashboard-readable). Writer helper: scripts/decision_log_append.py. Every entry carries a methodology_source field with one of 6 allowed values.
- **rationale:** Markdown for human read + commit diffs; JSON for dashboard widget + Cowork raw-GitHub reads. methodology_source forces each decision to declare its provenance (directive vs methodology vs override vs audit vs infrastructure vs legacy), preventing decision drift.
- **alternatives_considered:**
  - Markdown only (rejected — no dashboard widget)
  - JSON only (rejected — loses the open-in-editor affordance)
- **audit_finding_refs:** `Session 5 NEW-3`
### Amendment 5 (GCC universe) deferred to Phase 2 infrastructure

- **ts_utc:** `2026-05-22T16:02:00+00:00`
- **methodology_source:** `framework_directive`
- **decision:** GCC universe expansion (Saudi/Kuwait/Qatar/Bahrain halal aggregate ~400-500 tickers) is a Phase 2 infrastructure build. Does NOT consume Phase 1 slots and is NOT required during the 6-month freeze. UAE-only certification remains the deployment gate for any GCC-discovered strategy.
- **rationale:** GCC universe construction is multi-week (sourcing halal lists per market, OHLCV caching, integrity checks). Phase 1 cap=4 leaves no room for new strategy enrollment, so the only 'new Phase 1 work' the framework permits is infrastructure. Ahmed clarified GCC is Phase 2 work.
- **alternatives_considered:**
  - Build GCC universe in Phase 1 (rejected — multi-week build collides with the 6-month freeze on framework)
  - Defer indefinitely (rejected — Phase 2 review is when GCC gets considered for activation)
### Amendment 1 auto-execution layer deferred to Phase 2

- **ts_utc:** `2026-05-22T16:02:30+00:00`
- **methodology_source:** `framework_directive`
- **decision:** Auto-execution layer (orders fire to broker API without human approval per signal + idempotency + slippage + fail-safe halt) is a Phase 2 infrastructure build. Does NOT consume Phase 1 slots. Amendment 1 (relaxed WR floor for trend-following / breakout / momentum strategies under dSharpe ≥1.5 + PF ≥2.0 + CI positive) stays DORMANT until both auto-execution exists AND passes its own validation.
- **rationale:** Human-in-the-loop execution can't reliably honor a strategy with 4-of-5 losing trades (humans deviate; once execution deviates, certified expectancy no longer applies). Auto-execution is a major build (broker API, slippage modeling, fail-safe halt). Ahmed clarified it's Phase 2 work.
- **alternatives_considered:**
  - Activate Amendment 1 with current Telegram-tap workflow (rejected — workflow is human-in-loop, exactly what Amendment 1 forbids)
  - Drop Amendment 1 entirely (rejected — keeps the low-WR option available for Phase 2 when infrastructure exists)
- **audit_finding_refs:** `Session 5 Concern 4`
### Apply Improvement 5 — hide tickers with OOS n<30 from per-ticker displays

- **ts_utc:** `2026-05-22T16:20:08+00:00`
- **methodology_source:** `framework_directive`
- **decision:** generate_dashboard.py per-ticker WINNERS/LOSERS filter raised from oos_n>=10 to oos_n>=30. Removes DXCM-∞-expectancy and other sub-n=30 outliers from dashboard views. Portfolio verdict remains binding.
- **rationale:** Per Phase 1 directive Part 2 Improvement 5. n<30 estimates are dominated by sampling noise; the right fix is exclusion, not better formatting.
- **alternatives_considered:**
  - Keep at n>=10 with footnote (rejected — misleading by default)
  - Hide only oos_n<30 from WINNERS but show in LOSERS (rejected — asymmetry confuses interpretation)
- **audit_finding_refs:** `Session 5 NEW-1 related cherry-picking pattern`
### Improvement 1 — replace cherry-picked watch list with full cleared universe

- **ts_utc:** `2026-05-22T16:21:45+00:00`
- **methodology_source:** `framework_directive`
- **decision:** universe/divergence_us_paperforward_watchlist.txt now contains all 1,030 contributing US Divergence tickers from the PORTFOLIO_CLEARED run. Option A from Phase 1 directive Part 2 Improvement 1. WATCH_LIST_METHOD constant updated in scripts/paper_forward_divergence.py.
- **rationale:** Per Phase 1 directive: Option A is preferred — deploy detector across the ENTIRE cleared US Divergence universe rather than any selectively-sampled subset. Supersedes the 2026-05-21 random-sample seed=42 n=50, which itself superseded the original top-5-by-expectancy that audit NEW-1 flagged as cherry-picking.
- **alternatives_considered:**
  - Option B stratified sample with sector/liquidity caps (kept available; can switch later if Telegram noise becomes operationally unworkable)
  - Maintain n=50 random sample (rejected — directive prefers full universe)
- **audit_finding_refs:** `Session 5 NEW-1`
### Fix watchdog false-fire loop — add cron_paused.flag intentional-pause check

- **ts_utc:** `2026-05-22T18:33:47+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** cc_watchdog.ps1 now checks scripts/cron_paused.flag at the top of every tick. If present, watchdog skips all crash detection, updates last_check_ts, sets mode=monitoring_paused, and exits cleanly. Created the flag with paused_at_utc 2026-05-22T16:00:00+00:00 reason phase1_directive_pending_candidate_2.
- **rationale:** Cron disabled at 16:00 UTC per Q2=A. Sentinel naturally aged because nothing was touching it. Watchdog interpreted silence as crash and fired 18 false recoveries between 16:11 and 17:57 UTC (sentinel age 32->105 min, recoveries cycling every 3-5 min, each completing exit=0 but never touching the sentinel). Root cause: watchdog had no signal source to distinguish off-by-design from broken.
- **alternatives_considered:**
  - Disable watchdog entirely while cron paused (rejected — loses safety net for true crashes)
  - Make watchdog query Cloud Routines state directly (rejected — MCP not accessible from Windows scheduled task)
  - Heuristic on missed_sprints.log absence (rejected — overlaps with successful normal operation)
### Fix crash_log.json JSON-via-argv encoding bug

- **ts_utc:** `2026-05-22T18:33:47+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** cc_watchdog.ps1 Append-CrashLogJson now writes the JSON to a temp file and passes --file to crash_log_append.py instead of argv. Eliminates the every-recovery JSON parse failure observed today.
- **rationale:** PowerShell argv handling of strings with curly braces and embedded quotes mangled the JSON in transit (error: Expecting property name enclosed in double quotes line 1 column 2). Result: crash_log.md got appended but crash_log.json did not for every recovery since the watchdog was installed.
- **alternatives_considered:**
  - Use stdin pipe to Python helper (rejected — works but temp file is simpler and the helper already supported --file)
  - Manually quote the JSON in PowerShell (rejected — fragile across PS versions)
### Flag-removal sequencing — retrospective on coordination gap

- **ts_utc:** `2026-05-23T10:29:40+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** Retrospective on cron_paused.flag removal at 2026-05-23T09:58:42Z by prior CC session. Flag and Cloud Routine aig-mode1-sprint are independent guards: removing the flag while the routine itself remains disabled leaves the watchdog blind to an intentional pause with no sentinel-refresher behind it. Resulting v1.4 behavior: 3 false-fire recoveries at 10:04/10:09/10:16 UTC, capped at 5/5 by Fix 3 daily-cap. No user-foreground CC processes killed — kill-event analysis: 10:04 cleanup reached only Access-Denied kernel processes (edge-case PID-0 identification bug worth tracking but harmless because Windows refused), 10:09 cleanup reaped a 12-PID claude-code process tree (parent pid=18152 + 11 descendants) spawned in the 37-second inter-recovery window, 10:16 cleanup produced no descendant kill lines. v1.4 CommandLine filter at scripts/cc_watchdog.ps1:200-209 correctly excluded Claude Desktop from all three cleanups.
- **rationale:** Surface the coordination requirement explicitly so future flag-removal workflows pair flag-delete with Cloud Routine re-enable verification. v1.4 daily cap working as designed prevented v1.0-style runaway — without it this would have replayed the 2026-05-22 18-fire incident at larger scale. Cap operation is the only piece of v1.4 that got a real-world unsupervised test today, and it held.
- **alternatives_considered:**
  - Leave the 2026-05-22T18:33 decision as the only record (rejected — that entry's design-intent is correct but does not capture today's failure-mode demonstration that the coordination is load-bearing)
### Recreate cron_paused.flag (Option A) restoring coordinated pause

- **ts_utc:** `2026-05-23T10:29:41+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** scripts/cron_paused.flag recreated with paused_at_utc=2026-05-23T10:29:40Z. Watchdog v1.4 will see flag on next 60s tick and transition effective mode to monitoring_paused. Daily cap (5/5) remains in state.json until UTC midnight reset but is now redundant — flag short-circuits stale detection before cap evaluation. Cron re-enable remains a SEPARATE pending decision contingent on SKILL.md rewrite completing Part 4 three-filter methodology enforcement per Phase 1 framework directive.
- **rationale:** Smallest reversible action that restores design-intent from 2026-05-22T18:33 decision. Defers the deeper cron re-enable question as a separate deliberate choice after SKILL.md rewrite is complete. Option B (re-enable cron now) rejected because next fire could enroll Strategy 14 under forbidden Version-B methodology. Option C (do nothing, wait for cap reset at UTC midnight) rejected because it leaves a 9.5h window with no real-crash recovery and the same false-fire loop would recur tomorrow at cap reset.
- **alternatives_considered:**
  - Option B: re-enable Cloud Routine aig-mode1-sprint (rejected — SKILL.md rewrite not complete; cron firing now could enroll Strategy 14 under forbidden Version-B methodology per Phase 1 directive Part 4)
  - Option C: do nothing, let daily cap expire at 2026-05-24T00:00 UTC (rejected — 9.5h window with no crash protection; same loop recurs tomorrow at cap reset)
### 33d93ae commit message overstated completion -- SKILL.md never landed in code

- **ts_utc:** `2026-05-23T10:59:28+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** Record that commit 33d93ae's message asserted "aig-mode1-sprint SKILL.md rewritten directive-compliant" as part of the Phase 1 directive landing, but git evidence shows no SKILL.md was ever added, modified, or deleted in any commit in this repo's history. Phase 1 directive Part 8 actual status was 4/10 implemented in code at commit time, not 5/10 as the message implied.
- **rationale:** Cowork audit cycle reads commit messages on raw GitHub and would treat the assertion as fact. Capturing the drift in decision_log makes the audit chain self-correcting rather than self-reinforcing. The pause + decision-log infrastructure + Improvement 1 + Improvement 5 all landed honestly; the SKILL.md rewrite was asserted in prose but never implemented. SKILL.md rewrite (Task 2 this session) closes the actual gap.
- **alternatives_considered:**
  - amend the original commit (rejected -- rewriting history breaks audit chain integrity)
  - ignore and rewrite SKILL.md silently (rejected -- drift goes unrecorded, future audits cannot distinguish 'never done' from 'done but undocumented')
### Correction to entry 21 -- SKILL.md DOES exist at user-scoped scheduled-tasks path; prior conclusion was scope error

- **ts_utc:** `2026-05-23T11:58:57+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** Retract entry 21's central conclusion. Entry 21 (committed 2026-05-23 in commit 2bfa0af, ts_utc 2026-05-23T10:59:28Z) stated that 'no SKILL.md was ever added, modified, or deleted in any commit in this repo's history' and concluded that 33d93ae's commit message therefore overstated completion. The premise was true; the conclusion was wrong. The SKILL.md exists at C:\\Users\\ahmed\\.claude\\scheduled-tasks\\aig-mode1-sprint\\SKILL.md (outside the AIG git repo, by Claude Code's scheduling-system design -- scheduled-task definitions are user-scoped, not project-scoped). MCP scheduled-tasks listing confirms the canonical path, cron 12,27,42,57 * * * *, enabled=false, lastRunAt 2026-05-22T16:03:28.671Z. The SKILL.md body line 6-11 explicitly identifies itself as 'AIG Phase 1 directive-compliant sprint ... Replaces the prior Version-B autonomous strategy-enrollment SKILL after Ahmed's 2026-05-22 framework directive.' The 33d93ae commit message assertion 'aig-mode1-sprint SKILL.md rewritten directive-compliant' was TRUE -- the rewrite landed into a file outside the AIG repo's tracking. No commit-message-vs-actual-contents drift exists for SKILL.md.
- **rationale:** Faithful audit chain requires correcting wrong conclusions, not only logging new ones. Entry 21 was based on a path-scope error: prior session's 'git log --all -- SKILL.md' and recursive search under aig_engine/ both correctly returned zero, but those searches were the wrong scope for scheduled-task definitions, which Claude Code stores in ~/.claude/scheduled-tasks/<taskId>/SKILL.md (user scope, not project scope). Cowork reading entry 21 on raw GitHub would form the wrong picture of system state (would conclude 'SKILL.md never landed, cron re-enable blocked indefinitely on a non-existent task'). This entry supersedes entry 21's conclusion. Append-only discipline preserves entry 21 as historical record; this entry is the authoritative current state on the SKILL.md question.
- **alternatives_considered:**
  - amend entry 21 in place (rejected -- violates append-only discipline; future readers would not know the original conclusion existed and was corrected)
  - amend the 2bfa0af commit (rejected -- same history-rewriting hazard flagged in entry 21 itself for the 33d93ae case; consistency with the discipline that entry 21 invoked)
  - leave entry 21 standing and update only the next SKILL.md-related entry (rejected -- entry 21 is the canonical-looking record on this topic; without explicit retraction downstream readers will treat its conclusion as truth)
### Cron re-enable -- Cloud Routine aig-mode1-sprint resumed after SKILL.md verification

- **ts_utc:** `2026-05-23T12:14:42+00:00`
- **methodology_source:** `ahmed_override`
- **decision:** Cloud Routine aig-mode1-sprint re-enabled after Ahmed's explicit approval of the 6-step unpause sequence. Preconditions verified this session: (1) SKILL.md at ~/.claude/scheduled-tasks/aig-mode1-sprint/SKILL.md exists and is directive-compliant per Q2 structured verification (7 items + 6 prohibitions all COVERED); (2) MCP-side description in sync with SKILL.md frontmatter (byte-identical); (3) sprint scripts do not enroll new strategies at runtime (grep of strategy_register references shows zero enforcement reads in sprint_catchup/paper_forward_divergence/staged_validate; single read in generate_dashboard for display rendering only); (4) sprint_catchup.py confirmed absorbed-by-pause semantics -- MISSED_FIRES count is reported but cap-at-4 lives in SKILL prompt, no replay queue, missed marks just logged. Sequence executed: step 0 this decision_log entry, step 1 last_sprint_fire.txt refreshed to current UTC, step 2 watchdog state.json reset (recoveries_today=0, mode=normal, recoveries_today_date=2026-05-23), step 3 cron_paused.flag renamed to cron_paused.flag.removed-20260523T121442 for evidence preservation, step 4 MCP scheduled task enabled=true (next turn, after this commit lands cleanly).
- **rationale:** SKILL.md rewrite (stated precondition for cron re-enable per 2026-05-22T16:00:00Z pause decision) has been verified complete this session. Entry 22 retracted entry 21's incorrect 'SKILL.md never landed' conclusion based on path-scope error; the file has been in place at the user-scoped Claude Code scheduled-tasks path since 33d93ae. The 6-step sequence accounts for the latent state from the v1.4 cap-incident (sentinel stale ~17.5h, recoveries_today=5/5, mode=capped) by manually refreshing each piece rather than waiting for UTC-midnight auto-reset; 12h vulnerability window with no crash recovery declined when manual reset is one JSON edit.
- **alternatives_considered:**
  - Wait for UTC midnight (~12h from now) for watchdog cap auto-reset (rejected -- 12h with no crash recovery is unnecessary exposure when manual reset is one JSON edit)
  - Re-enable without sentinel refresh (rejected -- watchdog would fire recovery in the gap between flag removal and first cron tick, contradicting the deliberate unpause)
  - Defer cron re-enable until after watching one sprint fire under flag-still-present mode (impossible -- sprint cannot fire while paused on MCP side)
### Sprint burst killed by watchdog -- SKILL.md missing --mark-done between catch-up iterations

- **ts_utc:** `2026-05-23T13:20:16+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** First cron fire post-unpause at 2026-05-23T12:35:25Z executed a long catch-up burst (~70 missed fires absorbed; SKILL agent capped at 4 catch-up iterations + 1 current = 5 sprint cycles, each up to 10 min). SKILL.md does NOT instruct the agent to call sprint_catchup.py --mark-done between iterations; the script's docstring at sprint_catchup.py:247 says 'after each catch-up iteration (or at the end of the loop)' -- agent discretion. Agent did not refresh sentinel between iterations, so last_sprint_fire.txt stayed at 2026-05-23T12:14:42+00:00 (the manual touch from cron-re-enable step 1). At 12:44:42Z the sentinel hit the watchdog's 30-min stale threshold; v1.4 correctly detected stale-check 3/3, fired CRASH CONFIRMED, killed the in-flight sprint, then continued firing recoveries until manual cron_paused.flag recreation at 13:07Z stopped the loop. Belts-and-suspenders: MCP-side Cloud Routine aig-mode1-sprint set enabled=false in addition to the local cron_paused.flag. SKILL.md patch (add explicit --mark-done call between iterations or at top of each sprint step) and clean re-unpause deferred to next fresh session.
- **rationale:** The architecture's safety mechanism worked correctly -- v1.4 watchdog detected a genuine stale sentinel and acted per design. The failure surface was the SKILL prompt being ambiguous about sentinel-refresh discipline. The script comment at sprint_catchup.py:247 leaves the question to agent interpretation; the agent chose the conservative 'at end of full burst' pattern, which is incompatible with a long catch-up window where the burst itself exceeds the 30-min stale threshold. Surfacing this as a SKILL gap prevents reading the watchdog's intervention as a v1.4 bug -- it isn't, it's the absence of an explicit instruction in the SKILL. The flag-then-MCP disable belts-and-suspenders pattern matches the lesson from entries 21/22: flag and Cloud Routine are independent guards, both should be in the same state for unambiguous pause.
- **alternatives_considered:**
  - Recreate flag only, leave MCP enabled (rejected -- repeats the entries 21/22 coordination mistake in reverse direction; if flag gets removed in a future session without MCP awareness, Cloud Routine would resume against an unfinished investigation)
  - Edit SKILL.md inline this session and re-unpause immediately (rejected -- session is long, edit-then-test sequence deserves a fresh session with proper verification; cron stays disabled is the safe state until the fix lands)
  - Disable watchdog instead of cron (rejected -- watchdog acted correctly here; disabling it would expose to real future crashes for no benefit)
  - Patch sprint_catchup.py to self-touch sentinel on --notify-start (rejected for this session -- changes script semantics; safer fix is in SKILL.md which is operational instruction not engine code)
### WCK US 1D PORTFOLIO_CLEARED via orphan-spawn -- anomalous trigger, pre-registered gate intact

- **ts_utc:** `2026-05-24T06:32:00+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** Record that commit 687fd7e (`Sprint 2026-05-23_17-57`) finalized WCK US 1D at 1603/1603 tickers (100% sweep), trades=16621, dSharpe=1.2437, verdict PORTFOLIO_CLEARED_FOR_PAPER_FORWARD. The fire that produced this commit originated from a watchdog-recovery-spawned headless `claude -p` worker, NOT from a scheduled cron tick. At 17:57Z Cloud Routine `aig-mode1-sprint` was disabled MCP-side and the local `scripts/cron_paused.flag` was in place (both restored at 13:07Z per entry 24's belts-and-suspenders pause). The recovery worker had been spawned during the 12:44Z-13:07Z watchdog crash-confirm loop, persisted into the post-pause window, and ran its SKILL through to completion -- final WCK batches drained, finalize-on-100% triggered, commit landed, push succeeded. The trigger pathway is off-design; the work executed under it is methodologically identical to a normal sprint: frozen `config_hash` binding intact, frozen WCK spec per `strategy_register.md` row 13 intact, no parameter touches, no gate amendment, single finalization at 100% sweep. WCK retains its Pre-Framework tag per the 2026-05-22T15:58:30Z entry; the PORTFOLIO_CLEARED verdict is retained as historical record. WCK remains eligible for Phase 1 selection if the three-filter methodology mechanically selects it in a future selection cycle -- this entry does not pre-empt or foreclose that path.
- **rationale:** Two facts must coexist on the audit chain. (1) The trigger was an architecture gap (companion entry below documents the gap as a system property). (2) The data is methodologically clean. Conflating them would mean either discarding valid pre-registered evidence on procedural grounds or letting the architecture gap go undocumented for Cowork's next read. Recording both, and linking them explicitly, preserves gate discipline AND surfaces the architecture gap that Task C remediates. Cowork reading raw GitHub will see 687fd7e's commit message claim a Pre-Framework PORTFOLIO_CLEARED and find this entry explaining why the SKILL execution was methodologically sound despite the anomalous trigger.
- **alternatives_considered:**
  - Discard 687fd7e and re-run WCK from a normal cron tick post-restart (rejected -- the run honored the pre-registered config_hash and the 100% sweep rule, no post-hoc parameter change occurred, a re-run would be spec-identical and burn compute; the discipline that matters -- 'no post-hoc parameter change on a failing or in-flight dataset' -- was not breached)
  - Log clearance without linking to the architecture-gap entry (rejected -- audit-chain coherence requires the architectural framing be discoverable one click away from any reader who lands on this entry)
  - Tag WCK as `PORTFOLIO_TAINTED_BY_TRIGGER` (rejected -- 'tainted' would imply the gate's discipline was breached; orphan-spawn is an OPS/scheduler issue, not a methodology issue)
### Watchdog-recovery-spawn architecture gap -- pause guards stop new spawns, in-flight orphans run SKILL to completion

- **ts_utc:** `2026-05-24T06:32:30+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** Surface a system property exposed by the 12:44-17:57Z timeline on 2026-05-23. The three pause guards in current operation -- `scripts/cron_paused.flag`, MCP-side `aig-mode1-sprint enabled=false`, and the watchdog's flag-aware short-circuit (per entry 17, 2026-05-22T18:33:47Z) -- collectively stop NEW spawns: cron will not fire, MCP will not trigger, the watchdog will not initiate fresh crash-recovery workers. They do NOT stop already-spawned headless `claude -p` recovery workers from running their SKILL.md to completion. Once a recovery worker has been launched (typically a 10-15 min headless run), no in-process signal currently exists to interrupt it short of force-killing the PID tree. The companion WCK finalization entry above is one concrete instance of this gap. Overnight stability evidence (2026-05-23 evening through 2026-05-24 morning): a 16-PID claude-code-related process pile was identified at session start. 11 of those were Claude Desktop processes (left alive, not orphans). 7 were CC workers, killed individually with per-PID verification (none respawned). Process count went 16 -> 9 and held at 9 with zero respawn. Watchdog log showed continuous skip-paused entries from 13:07Z through this morning's 06:02Z with zero crash-confirm triggers. Zero new sprint commits, zero new sentinel touches, zero new Telegram messages were generated between 17:57Z and reaping time. This bounds the gap as ONE-SHOT -- orphans exit cleanly after SKILL completion, they do not persist as daemons and do not re-spawn. Risk window per orphan = one SKILL execution (~10-15 min); risk window per pause incident = (orphans spawned during the kill-burst) * (orphan SKILL duration).
- **rationale:** Naming a system property is the precondition for closing it. The 2026-05-22T18:33:47Z fix added `cron_paused.flag` as a watchdog signal but framed pause as 'stop new fires'. The WCK orphan-spawn revealed that 'in-flight workers' is a third category alongside cron-driven fires and watchdog-driven recoveries. Recording the gap as ONE-SHOT is load-bearing -- it means future pause incidents do not require unbounded vigilance, they require a finite drain wait. Cowork reading the audit chain should see this gap stated explicitly, not inferred from a sequence of incident entries. Remediation options (NOT implemented this session, future work): (a) recovery workers consult `cron_paused.flag` at SKILL preamble and self-exit, (b) explicit kill-orphans step in pause workflow, (c) Cloud Routine architecture replaces headless-orphan recovery with sentinel-only signaling.
- **alternatives_considered:**
  - Defer documenting until a remediation is implemented (rejected -- audit chain self-correctness requires the gap be visible BEFORE the fix lands so Cowork sees the system honestly)
  - Treat the WCK orphan as one-off and not generalize (rejected -- the same architectural shape will recur on any future pause + still-running-worker overlap; one-off framing would force the next reader to re-derive the property)
  - Disable the watchdog entirely to eliminate the spawn-source (rejected -- watchdog acted correctly in the 2026-05-23 incident; the gap is in pause-coordination, not in watchdog detection)
### Append to entry 24 (2026-05-23T13:20:16Z) -- catch-up cap x iteration budget exceeds stale threshold by design

- **ts_utc:** `2026-05-24T06:33:00+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** Add to entry 24's analysis the explicit arithmetic that makes the SKILL gap binding regardless of agent discretion. Catch-up cap = 4 iterations; per-iteration sprint budget = ~10 min; max catch-up window = 4 x 10 = ~40 min. Watchdog stale-sentinel threshold = 30 min. 40 > 30 by 33%. A fully-utilizing cap-bound catch-up burst will exceed the stale threshold even if every iteration is healthy and progressing. Entry 24 framed the missing `--mark-done` as 'agent discretion' (sprint_catchup.py:247 docstring leaves the question to the agent); this entry sharpens that to: agent discretion CANNOT resolve the timing conflict -- the budget math forecloses any safe choice that does not refresh the sentinel mid-burst. Therefore the SKILL.md patch in Task C is necessary, not optional. Without explicit `sprint_catchup.py --mark-done` between iterations, the SKILL race-loses to the watchdog by design on long catch-up windows.
- **rationale:** Entry 24 documents the WHAT (sprint killed because sentinel went stale during burst); this entry documents the WHY in arithmetic the next reader can verify without re-deriving. Existence-proof analysis ('one incident occurred') is weaker evidence for patch necessity than design-proof analysis ('math forecloses any non-patching choice'). Cowork reading the chain would see entry 24 say 'agent did not refresh sentinel between iterations' and could reasonably ask 'why didn't the agent just refresh?'. This entry answers: the agent COULD have refreshed; the patch makes that the default instead of leaving it as discretion. Both entries together close the question.
- **alternatives_considered:**
  - Edit entry 24 in-place to add the math (rejected -- append-only discipline; entry 24 is in commit c92d080 on origin/main; rewriting would violate the same discipline invoked at entries 21/22)
  - Skip the entry and put the math only into the SKILL.md patch commit message (rejected -- the decision log is the canonical Cowork audit surface; commit messages are downstream documentation, not primary audit chain)
  - Wait until Task C lands and write both entries together (rejected -- decision log is append-only and timestamps are monotonic; backdating to align with the patch commit would obscure temporal order)
### Correction to entries 25/26 (17:57Z timezone label error) + 97cdf93 named as second orphan-spawn instance + 687fd7e flag-creation noted

- **ts_utc:** `2026-05-24T06:50:00+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** Three Task-B-derived corrections on the 2026-05-23 orphan-spawn incident record. (a) **Timezone label error in entries 25 and 26.** Three instances of `17:57Z` across the two entries incorrectly attach a UTC `Z` suffix to digits that come from the LOCAL GST sprint-name convention. The actual UTC time of commit 687fd7e is `2026-05-23T13:57:09Z`; the local GST time is `2026-05-23T17:57:09+04:00`. The digits `17:57` are correct when used as the sprint identifier (`Sprint 2026-05-23_17-57`, retained verbatim from the commit message) but wrong when suffixed with `Z`. Specific instances: entry 25 decision -- `At 17:57Z Cloud Routine aig-mode1-sprint was disabled MCP-side`; entry 26 decision -- `the 12:44-17:57Z timeline on 2026-05-23`; entry 26 decision -- `between 17:57Z and reaping time`. All three should be read as `13:57Z` (UTC) or equivalently `17:57+04:00` (GST). The authoritative UTC timeline for the 2026-05-23 incident is: 12:14:42Z manual sentinel touch + cron unpause prep (entry 23); 12:34:51Z c6dec3d sprint commit; 12:44:42Z watchdog crash-confirm and burst kill (entry 24); 12:44-13:07Z watchdog recovery-spawn loop; 13:07Z manual cron_paused.flag recreation halting the loop; 13:07:26Z 97cdf93 sprint commit (first orphan-spawn artifact, see (b)); 13:20:17Z c92d080 entry-24 documentation commit; 13:57:09Z 687fd7e WCK finalization commit (second orphan-spawn artifact). Entries 25 and 26 remain unedited on origin/main per append-only discipline; this entry is the authoritative timeline. (b) **97cdf93 named as second orphan-spawn instance.** Entry 25 names only 687fd7e as a concrete orphan-spawn artifact; entry 26 frames the architecture gap as a system property without naming 97cdf93. Commit 97cdf93 (committed `2026-05-23T13:07:26Z` per git author date) landed at-or-just-after the manual flag recreation, ~50 minutes before 687fd7e in the timeline. Work product is clean: full WCK batches 6 and 7 (1600/1603 = 99.8% sweep) and paper-forward delta +23/-5 (18 open, 5 history). Trigger is anomalous: commit time is post-kill (kill at 12:44:42Z) so the producing worker was a watchdog-recovery-spawn or persistent orphan rather than the post-unpause cron burst (which was killed). 97cdf93 is therefore the FIRST orphan-spawn artifact on 2026-05-23 (by commit time); 687fd7e is the SECOND, ~50 min later. Entry 26's `(orphans spawned during the kill-burst) * (orphan SKILL duration)` risk-window formula covers both artifacts; this entry just names the specific commits so future readers do not have to derive the set from git timestamps. (c) **687fd7e unmessaged flag-file creation.** Commit 687fd7e incidentally creates `scripts/cron_paused.flag` as a new empty file (`new file mode 100644`, blob `e69de29`) without naming it in the commit message. Inverse direction of the 33d93ae drift documented in entry 21: 33d93ae claimed a change that did not land in the repo; 687fd7e landed a change that was not claimed in the message. Severity low -- the change is just the pause-flag the operator manually recreated locally at 13:07Z (per entry 24) being correctly tracked in git via the sprint's `git add` step. Naming it here closes the audit chain on 'when did the flag get re-tracked in git after the 64db7d7 rename' without forcing future readers to derive the answer from the diff alone.
- **rationale:** Append-only discipline requires correction entries when load-bearing audit-surface text is wrong. The three sub-fixes above are bundled into one entry because they share temporal and topical context (all relate to the 2026-05-23 orphan-spawn incident and its decision_log treatment): a reader landing on any one of (a)-(c) finds the others one screen away, and future Cowork raw-GitHub reads see a single self-contained correction record rather than three scattered entries. (a) is the most load-bearing -- a 4-hour audit-timeline error in two consecutive entries would otherwise propagate into future incident reconstructions and contradict the watchdog log / git timestamps the next auditor would cross-reference. (b) closes the implicit-vs-explicit gap (entry 26's framing covers 97cdf93 categorically, but explicit naming prevents the next reader from missing the specific instance). (c) preserves the 'no unclaimed mutations' discipline parallel to the 33d93ae lesson, inverted direction.
- **alternatives_considered:**
  - Three separate decision_log entries, one per sub-fix (rejected -- temporal/topical coherence of the 2026-05-23 incident set makes one bundled correction more legible than three scattered records; entries 21/22 precedent shows a single-topic correction entry is the established convention, but this incident's three sub-findings are all instances of the same audit-chain hygiene problem so one entry is the closer match)
  - Edit entries 25 and 26 in-place to fix the '17:57Z' label (rejected -- violates append-only discipline invoked at entries 21/22; the precedent says corrections REPLACE the original conclusion via a new entry, not by mutating the original)
  - Skip sub-fix (c) on the 687fd7e flag-file creation (rejected -- the 33d93ae lesson at entries 21/22 explicitly calls out commit-message-vs-content drift; including the inverse-direction case keeps the audit chain symmetric and prevents a future reader from concluding 'drift in only one direction is recorded' from a non-symmetric chain)
  - Include a sub-fix (d) on c6dec3d trigger timing (rejected -- diagnostic data this session revealed c6dec3d, 97cdf93, and 687fd7e ALL lack normal-cron-sprint housekeeping fingerprints (no sentinel touch, no Telegram send, no missed_sprints update), pointing to a category-level no-housekeeping execution path rather than a single timing imprecision; deferred to a future fresh-budget session as a separate investigation requiring reads of session_resume_prompt.txt, the sprint_catchup.py SKILL preamble, and the watchdog spawn command, rather than bundling a hasty conclusion with the three solid corrections in this entry)
### SKILL.md patched -- per-iteration sprint_catchup.py --mark-done; catch-up sentinel-stale bug closed; no-housekeeping execution path remains deferred

- **ts_utc:** `2026-05-24T07:05:00+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** This entry records the SKILL.md patch applied 2026-05-24 to close the cron-triggered catch-up burst sentinel-stale bug captured in entry 24 (`2026-05-23T13:20:16Z`, commit c92d080) and analyzed arithmetically in entry 27 (commit 22792bc). **Patch.** Modifies the iteration-structure prose in step 1 of `C:\Users\ahmed\.claude\scheduled-tasks\aig-mode1-sprint\SKILL.md`. Adds explicit loop wrapping (`Run steps 2-5 below min(N, 4) + 1 times total`) and inserts a sentinel-refresh call (`python scripts/sprint_catchup.py --mark-done`) at the START of each iteration body, including iteration 1. Also disambiguates the prior `Cap at 4 iterations/fire` line to `Cap at 4 catch-up iterations + 1 current = 5 total sprint cycles per fire` matching entry 24's framing. Includes inline rationale referencing entries 24 and 27 so future readers of SKILL.md see both the what (call --mark-done per iteration) and the why (cap times budget exceeds stale-threshold without refresh). **Scope: CRON-TRIGGERED CATCH-UP BURST SENTINEL-STALE BUG ONLY.** Without per-iteration sentinel refresh, the 4-iter cap times ~10-min budget reaches ~50 min and exceeds the watchdog's 30-min stale-sentinel threshold, causing the watchdog to kill the running sprint burst mid-iteration (as observed `2026-05-23T12:44:42Z`). With per-iteration refresh at iteration START, the watchdog sees stale age <= one iteration runtime (~10 min) and will not fire spuriously during the burst. Start-of-iteration placement (rather than end-of-iteration) is the monotonically safer choice: if the sentinel was already ~25 min stale at fire entry, a 10-min iteration ending at 35 min stale would trigger the watchdog mid-iteration -- start-of-iteration refresh eliminates that vulnerability. **Scope: this patch does NOT address the no-housekeeping execution path** used by watchdog-recovery-spawned workers. That category-level finding -- the three sprint commits on 2026-05-23 (c6dec3d, 97cdf93, 687fd7e) share a no-sentinel-touch / no-Telegram / no-missed_sprints-update fingerprint -- is a separate bug class deferred to a future fresh-budget session per entry 28's `alternatives_considered` item 4. The deferred investigation requires reads of `scripts/session_resume_prompt.txt`, the sprint_catchup.py SKILL preamble path, and the watchdog's exact spawn command in `scripts/cc_watchdog.ps1`. **Both fixes needed for full coverage.** This patch covers normal cron-fired sprints; the deferred investigation covers watchdog-recovery-spawned workers. Until the second fix lands, orphan-spawn artifacts may still produce sprint commits without proper housekeeping touches. **SKILL.md location.** Outside the aig_engine git repo at user-scoped `C:\Users\ahmed\.claude\scheduled-tasks\aig-mode1-sprint\SKILL.md` per Claude Code's scheduling-system design (see entry 22). The SKILL.md edit therefore produces no git artifact; this decision_log entry IS the audit-chain record of the patch. Re-unpause sequencing (cron_paused.flag removal + MCP enable=true) is deferred to a separate session with explicit checkpoints -- the cron-paused state from 2026-05-23T13:07Z remains in effect. **Verification step queued for the re-unpause session (not this one):** confirm Claude Code reads SKILL.md fresh on each cron fire vs caching it between fires. The simplest check after applying the SKILL.md edit is to inspect MCP `list_scheduled_tasks` output for `aig-mode1-sprint` -- if the description field reflects the updated text, the harness has loaded the new SKILL; if it still shows the old description, a cache may need clearing before re-unpause is safe.
- **rationale:** The audit chain requires a concrete record when a documented bug gets patched, especially when the patched file lives outside the git surface (so the audit chain cannot rely on `git log` to find the change). Without this entry, a future Cowork audit would see entries 24/27/28 documenting the bug + corrections but no record that the bug was actually fixed. Entry 29 closes that loop. The scope-carving language (what the patch does and does not cover) prevents a future reader from assuming the SKILL.md patch is the complete fix -- it is not; orphan-spawn artifacts via the recovery-spawn path are still possible until the deferred investigation lands its own fix.
- **alternatives_considered:**
  - Skip decision_log entry, rely on chat-transcript memory of the patch (rejected -- audit-chain integrity requires a permanent record; chat transcripts are not part of the audit surface and Cowork reads only the git-tracked decision_log)
  - methodology_source = infrastructure_decision (defensible alternative -- the patch IS an infrastructure/tooling fix; chose audit_finding_resolution because the bug was specifically documented in entries 24/27/28 and this patch closes that documented finding, which fits the enum's intent more tightly; CEO leaned this direction in the approval message)
  - Commit SKILL.md into the aig_engine repo for direct git tracking (rejected this session -- would require rethinking Claude Code's user-scoped scheduled-tasks design; not in scope for Task C. Could be considered as a future infrastructure decision when the scheduling architecture is revisited)
  - Apply the patch without writing a decision_log entry (rejected -- inconsistent with the audit-chain discipline that documented the bug; the patch resolution must be recorded in the same surface where the bug was named)
  - End-of-iteration --mark-done placement instead of start-of-iteration (rejected -- end-of-iteration leaves iteration 1 vulnerable when the sentinel was already stale at fire entry; start-of-iteration is monotonically safer and was confirmed by CEO this session)
- **audit_finding_refs:** `entry 24 (2026-05-23T13:20:16Z)`, `entry 27 (2026-05-24T06:33:00+00:00)`, `entry 28 (2026-05-24T06:50:00+00:00)`
### Correction to entry 29 -- MCP description field reflects SKILL.md frontmatter, not body; entry 29's verification mechanism was structurally flawed

- **ts_utc:** `2026-05-24T07:30:00+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** On 2026-05-24 (post-Task-C entry 29 commit, this session), called MCP `list_scheduled_tasks` to perform the cache-verification step queued in entry 29. The returned description field for `aig-mode1-sprint` was: "AIG Phase 1 directive-compliant sprint -- drains existing queue, runs paper-forward detector, regenerates dashboard, commits. No autonomous strategy enrollment. Strategy slots driven by the three-filter methodology in strategy_register.md, not by queue-emptiness." This is byte-identical to the SKILL.md YAML frontmatter `description:` line (a fact already established in entry 23, 'MCP-side description in sync with SKILL.md frontmatter (byte-identical)'). The patch in entry 29 modified the BODY of SKILL.md (the iteration-structure block inside step 1), not the frontmatter. Therefore the MCP description field could never reflect the patched body content, regardless of whether the MCP scheduled-tasks subsystem caches SKILL.md body between fires. The verification mechanism proposed in entry 29 ('inspect MCP list_scheduled_tasks output for aig-mode1-sprint -- if the description field reflects the updated text, the harness has loaded the new SKILL') was structurally unable to verify what it claimed. This is the same class of audit-chain drift as the 33d93ae lesson (entries 21/22): a load-bearing audit-surface claim that does not match the underlying reality. **Authoritative correction.** Any future SKILL.md patch verification that aims to test cache state must either (a) include a frontmatter `description:` bump so the MCP-exposed field changes visibly, (b) restart Claude Code to force MCP runtime cache invalidation, or (c) observe first-fire behavior directly. The MCP description-field-changed criterion alone is insufficient for body-only patches. **What IS verified about the entry 29 patch.** The Edit tool's success message confirms the patch landed on disk in SKILL.md ('The file ... has been updated successfully'). The on-disk SKILL.md file content is the patched version. What remains unverified at the MCP layer is whether the scheduled-tasks subsystem will read that on-disk version at the next fire, or use a stale in-memory cached version. Entry 31 (next entry below, this commit chain) records the operational decision on how to proceed despite the unresolved cache question.
- **rationale:** Same audit-chain self-correction discipline invoked at entries 21/22: when a load-bearing claim in a prior entry turns out to be wrong, the correction is recorded as a new entry rather than by editing the original. Entry 29's verification step was clear enough that a future Cowork audit would have followed it literally and reached a wrong conclusion -- either 'description unchanged, cache OK, proceed' (optimistic misread) or 'description unchanged, cache stale, halt re-unpause indefinitely' (pessimistic misread). Either outcome would be based on a misunderstanding of what the MCP description field actually represents. Naming the flaw explicitly prevents that downstream error.
- **alternatives_considered:**
  - Edit entry 29 in-place to fix the verification step (rejected -- violates append-only discipline established at entries 21/22)
  - Skip the correction and just proceed with first-fire verification (rejected -- entry 29's verification step would remain a future-audit hazard; correcting it explicitly maintains audit-chain integrity)
  - Bump SKILL.md frontmatter description as part of this correction so the verification mechanism becomes usable retroactively (deferred -- if a body-only patch verification is needed in the future, the frontmatter-bump pattern can be considered as part of THAT patch's design; not retrofitting entry 29's patch this session)
- **audit_finding_refs:** `entry 29 (2026-05-24T07:05:00+00:00)`
### Cron re-enable (Option D) -- accept cache verification uncertainty; use first fire as verification mechanism; accept recovery-spawn architecture gap per entries 26/28

- **ts_utc:** `2026-05-24T07:31:00+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Re-enable cron Cloud Routine `aig-mode1-sprint` after Task C SKILL.md patch landed (entry 29) and the cache-verification mechanism in entry 29 was acknowledged structurally flawed (entry 30 above). **Option D verification path selected.** Use the first cron fire post-unpause as the verification mechanism. **Primary success criterion** (the only observable that uniquely distinguishes 'patched SKILL ran' from 'old SKILL ran'): `last_sprint_fire.txt` advances every ~10 min during the catch-up burst. The per-iteration sentinel touch is the specific behavior the entry 29 patch added; sentinel advancement is the direct observable of patch effect. **Corroborative signals** (confirm but do not uniquely prove): per-iteration `SENTINEL_UPDATED=<ts>` lines in the sprint agent's stdout (confirms the script-level call ran); no new 'CRASH CONFIRMED' entries in `crash_log.md` during the burst window (confirms watchdog did not need to intervene). **Necessary but not sufficient:** a clean sprint commit landing on `origin/main` at burst end. The old SKILL could also produce a commit if the burst happened to complete before the 30-min stale threshold (e.g., if the queue drained faster than expected and the burst exited early). Commit-landing alone does NOT prove the patch is in effect. **Failure criterion (preemptive, ~15-min headroom on watchdog's 30-min stale threshold):** if `last_sprint_fire.txt` has NOT advanced beyond Step 2's manual touch timestamp by ~15 minutes after the first cron fire begins (first-fire start visible via process spawn or first agent output), declare failure preemptively and recreate the flag BEFORE the watchdog stale-check fires. This gives 15-min headroom on the 30-min threshold and aborts the burst while still within the original sentinel's stale-safe window. Without this preemptive check, the failure signal would arrive ~30+ min into the burst (when watchdog fires CRASH CONFIRMED), well into burst execution and harder to abort cleanly. **Failure criterion (late detection, fallback):** sentinel does not advance mid-burst (old SKILL loaded from cache), watchdog fires CRASH CONFIRMED, recovery-spawn loop initiates. **In the failure case:** immediately recreate `scripts/cron_paused.flag` + MCP disable to halt the loop, then proceed to Option A (frontmatter description bump) for the next attempt. **Architectural acceptance.** The recovery-spawn no-housekeeping execution path (documented in entries 26/28/29) remains a known gap. If a recovery-spawn produces a sprint commit during the catch-up burst or subsequent fires (next 24-48 hours), it is accepted as known-pattern; entries will be updated to record it as part of the established gap, not treated as a new bug. **Pause-window context.** Pause established at 2026-05-23T13:07Z (entries 19/20); current pause duration ~22 hours. Cron `12,27,42,57 * * * *` with 347s jitter; ~22 hours / 15-min cadence = ~88 nominal missed fires. Catch-up cap is `min(MISSED_FIRES, 4) + 1` = 5 sprint cycles, each up to 10 min. With the entry 29 patch, each iteration starts by refreshing the sentinel via `python scripts/sprint_catchup.py --mark-done`, so the watchdog-visible stale age is bounded at ~10 min throughout the burst. First fire expected to complete in ~40-50 min wall-clock.
- **rationale:** Path A (genuine new Phase 1 strategies via three-filter methodology, frozen-before-data) requires autonomous sprint cadence to make progress; running sprints manually does not scale. The entry 29 patch closes the documented bug that broke the previous re-unpause (entries 23 -> 24). Option D accepts that the cache-loaded-or-not question is genuinely unresolved (per entry 30) but bounds the failure mode: if cache is stale, the result is the same kill-burst pattern from 2026-05-23, for which we now have an explicit recovery playbook (recreate flag + MCP disable). The watchdog v1.4 daily-cap (entry 18) provides a second-line safeguard. Failure cost: one wasted catch-up burst + one decision_log entry recording the cache-stale finding. Success cost: zero. Asymmetry favors attempting. The criterion restructuring (primary vs corroborative vs necessary-but-not-sufficient) addresses a previously-implicit conflation -- a commit landing is observable but does not uniquely prove patch effect, while sentinel advancement during the burst IS uniquely tied to the patched behavior. The preemptive failure criterion (~15-min sentinel-stale-check from the surveillance session) gives explicit abort headroom on the watchdog's 30-min threshold; without it, failure would only surface when the watchdog already started spawning recovery workers, which is harder to recover from cleanly.
- **alternatives_considered:**
  - Wait for a fresh session and attempt Option A (frontmatter description bump) first (rejected -- adds one more session of cron-paused time during which Phase 1 work cannot progress; the failure mode of Option D is bounded and recoverable in the same session)
  - Option B -- restart Claude Code to force MCP cache invalidation (deferred -- requires CEO to exit + relaunch CC, operational overhead this session does not have authority to require; held in reserve if Option D fails)
  - Skip re-unpause until the no-housekeeping execution-path investigation lands (rejected -- the no-housekeeping gap is independent of the cron-triggered path; cron-fired sprints with the entry 29 patch should not trigger the no-housekeeping pattern, only recovery-spawned workers do)
  - Re-unpause without supervision and accept whatever happens (rejected -- 2026-05-23 demonstrated that unsupervised post-pause cron resumption can produce hours of recovery-loop activity; supervised first-fire is the discipline this session has been building)
  - Use any-of-four success criteria framing as originally drafted (rejected -- conflates the uniquely-discriminating signal (sentinel advancement) with corroborative or necessary-but-not-sufficient signals; commit-landing alone could occur under either old or patched SKILL, so treating it as a success criterion would create a false-positive risk)
  - Use only the watchdog CRASH CONFIRMED as the failure signal (rejected -- that signal arrives ~30+ min into the burst, well past the point where clean abort is possible; the preemptive 15-min sentinel-check fires earlier and within the original sentinel's stale-safe window)
- **audit_finding_refs:** `entry 29 (2026-05-24T07:05:00+00:00)`, `entry 30 (2026-05-24T07:30:00+00:00)`
### Framework directive amendment -- per-market strategy caps; TradingView sources permitted with qualification criteria; 12-month coverage target; 6-month freeze broken 2 days in with explicit rationale

- **ts_utc:** `2026-05-24T10:00:00+00:00`
- **methodology_source:** `framework_directive`
- **decision:** Amends the Phase 1 framework directive committed 2026-05-22 (commit 33d93ae, embedded in strategy_register.md lines 41-211). Three substantive changes plus one timeline change. **Change 1 -- Phase 1 strategy cap (amends strategy_register.md lines 119-126).** Original cap of 4 candidates total replaced by per-market caps: US = 6 slots, UAE = 4 slots, Crypto = 3 slots, total 13. Divergence remains as US slot 1 (cleared via Session 5 framework selection, grandfathered, no re-validation required). All other slots (US 2-6, UAE 1-4, Crypto 1-3) are OPEN and require three-filter methodology selection plus validation gate passage to fill. The 12 Pre-Framework strategies (mbv, dbo, roc, vcb, hat, pmr, str, art, cmf, gap, wck, ema200) plus 1 grandfathered (divergence, which fills US slot 1) make up the 13 historical strategies in strategy_register.md lines 237-251. The 12 Pre-Framework strategies do NOT auto-fill any slot under this amendment -- including WCK's PORTFOLIO_CLEARED verdict from 2026-05-23 commit 687fd7e (orphan-spawn pathway, entries 25/28). Pre-Framework strategies may be re-tested under the amended gate per directive Part 3 (Method A fresh OOS or Method B post-2026-05-22 forward data); if a re-test passes, it fills a slot. **Change 2 -- Source priority (amends strategy_register.md lines 167-176 and 178-184).** Original source priority list (Lopez de Prado -> replication -> Tareq constrained) is expanded to include TradingView strategies meeting the following qualification criteria, ALL of which must hold: (A) public backtest visible on TradingView covering at least 5 years of data; (B) backtest run on a recognizable universe (S&P 500, Russell 1000, or equivalent index -- not cherry-picked tickers); (C) code/parameters visible enough to re-implement and pre-register against our own universe before any test against our data; (D) strategy published/visible for at least 6 months (avoid newly-published over-fits). The original Part 4 prohibition on "Scanning Pine forum top-returns lists" (strategy_register.md line 180) is replaced by: "Scanning TradingView strategies permitted ONLY if criteria A+B+C+D all hold." All other prohibitions remain in force unchanged: no parameter optimization across universe (line 181), no iterating on failed strategies (line 182), no CEO scanning past results to spot patterns (line 183), no Ahmed intuitions as inputs except via the Ahmed-input exception (line 184). The Ahmed-input exception (lines 186-191), data reuse policy (lines 193-196), and autonomous-execution scope (lines 198-212) all remain in force unchanged. The three-filter methodology (Filter 1 archetype diversity, Filter 2 evidence tier T1/T2/T3, Filter 3 infrastructure readiness -- lines 131-166) remains in force unchanged and applies to all new slot selection across all three markets. **Change 3 -- Coverage target.** Adds explicit 12-month coverage target: 75% of tickers in each market's shared universe showing winning trades under at least one cleared Phase 1 strategy. Target measured per market (US, UAE, Crypto each independently must hit 75%, not aggregated). Target is aspirational, not a hard deadline -- strategies still must pass the validation gate to count. If only N of 13 slots clear and the remaining slots fail honest tests, coverage stops at N's actual ticker count, not lowered gate to force coverage. **Change 4 -- Freeze acknowledgment.** Original directive (strategy_register.md lines 43-49) declared a 6-month freeze ending ~2026-11-22. This amendment breaks the freeze 2 days into its 6-month window. The break is acknowledged explicitly here rather than left implicit. Rationale: the original directive's goal was establishing the framework as substrate for autonomous Phase 1 operation, which has now landed (SKILL.md patched per entry 29, cron re-enabled per entry 31, first verified autonomous fire confirmed today). The new goal -- 75% ticker coverage across three markets within 12 months -- requires scaffolding the original directive was not designed for (per-market caps, broader source set). Continuing with the original cap of 4 would mean accepting that the new goal is unreachable under the original directive. Amending honestly is more honest than either pretending the original goal still applies or pretending the original directive still holds while violating it. **What is preserved.** Three-filter methodology (unchanged), validation gate (unchanged -- min_expectancy >= 1.0, dSharpe >= 0.5, min trades per universe), pre-registration discipline (unchanged -- config_hash before data), append-only audit chain (unchanged), decision_log infrastructure (entries 23+ per directive Part 7, unchanged), re-validation discipline (unchanged -- Method A/B for Pre-Framework strategies). **What this amendment does NOT change.** The walk-forward research track (entry 33, this commit chain) is a separate parallel effort and is NOT a framework amendment -- it operates outside both the original directive and this amendment under explicit boundary rules captured in entry 33.
- **rationale:** The original directive's cap of 4 strategies plus pure-source restrictions was designed to prevent the survivorship-bias and selection-bias pattern that produced the 13 Pre-Framework strategies. Those prevention mechanisms were correct for the original goal. But the original goal was narrow: establish framework discipline. The current goal -- coverage across markets -- requires more strategies and broader sources without abandoning the prevention mechanisms. The amendment threads this by: (a) raising the cap but applying it per-market (so quality control still applies per market), (b) permitting TradingView as source but with criteria A+B+C+D as the new quality bar replacing 'no scanning at all,' (c) keeping every other prohibition unchanged, (d) keeping the three-filter methodology unchanged so the selection mechanism that survived audit remains the selection mechanism going forward. The amendment is intentionally a *modification* of the directive, not a replacement -- Parts 1 (pause), 2 (improvements), 3 (re-validation discipline), 5 (Mode 1/2), 6 (Telegram), 7 (decision log), 8 (order of operations) all remain in force.
- **alternatives_considered:**
  - Stay within the original directive's cap of 4 (rejected -- math: 4 strategies times typical 20-30% per-market coverage = 60-80% upper bound, but only if strategies are diverse, and only if all 4 clear; expected coverage with realistic clear rates ~50%; this falls short of the 75% target and lying to ourselves about the math is worse than amending)
  - Raise cap to 8 total without per-market breakdown (rejected -- concentrates risk; if all 8 cleared strategies happen to be US-archetype-fits, UAE and Crypto get 0; per-market caps force diversification)
  - Raise cap to unlimited (rejected -- reopens the survivorship-bias loop the original directive was built to close; no upper bound on selection = no quality control)
  - Add TradingView without qualification criteria (rejected -- same as unlimited cap, opens the prohibited Pine-forum-scanning pattern under a different name)
  - Keep all prohibitions but expand to broader sources (rejected -- internally contradictory; either TradingView is a permitted source or it isn't; the qualification criteria A+B+C+D are the honest middle ground)
  - Defer this amendment until after the 6-month freeze ends ~2026-11-22 (rejected -- that costs 5.5 months of work that won't fit the goal; the cost of breaking the freeze honestly with documented rationale is lower than the cost of stalling)
  - Drop the 75% coverage target instead of amending (rejected -- the target was set by operator explicit decision in this session, with full understanding of the implications; the amendment matches the target rather than the target being adjusted to fit the framework)
- **audit_finding_refs:** `entry 8 (2026-05-22T15:58:00Z Phase 1 cap = 4 candidates set; Divergence grandfathered slot 1)`, `entry 17 (2026-05-22T18:33:47Z watchdog pause flag)`, `entry 23 (2026-05-23T12:14:42Z first re-unpause attempt)`, `entry 24 (2026-05-23T13:20:16Z sentinel-stale bug)`, `entry 29 (2026-05-24T07:05:00+00:00 SKILL.md patch)`, `entry 31 (2026-05-24T07:31:00+00:00 cron re-enable)`
### Walk-forward research track (Track 3) added as parallel separate effort -- boundary against framework directive (Track 1) and amendment (Track 2)

- **ts_utc:** `2026-05-24T10:01:00+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Establishes a third research track operating in parallel with but separate from the original framework directive (Track 1, strategy_register.md lines 41-212) and the framework amendment per entry 32 (Track 2). **Track 3 -- Walk-forward research on historical data.** Method: pick strategy + ticker + long-history dataset (>=5 years available). Split data into training period (years 1 through N minus 6 months) and held-out test period (last 6 months). Develop or backtest strategy on training period. Evaluate strategy performance on held-out test period. If performance on held-out period is consistent with training period, the hypothesis 'strategy is robust to held-out time windows' is supported (NOT confirmed as paper-forward -- see boundary rules below). If performance diverges, the hypothesis is rejected. **Purpose.** Test the philosophical hypothesis that strategies which perform well on held-out historical windows may also perform well in true forward time. This is a research effort. It is NOT a strategy clearance mechanism. **Boundary rules (the discipline that keeps Track 3 honest):** (1) Telegram signals from Track 3 carry the literal label "WALK-FORWARD RESEARCH SIGNAL -- NOT PAPER-FORWARD VERIFIED" -- operator must see this label every time. (2) Track 3 results live in a separate file (`walk_forward_research.json` or equivalent, to be defined when Track 3 first runs), NOT in `strategy_register.md` or `paper_forward_positions.json`. (3) Track 3 strategies do NOT auto-promote to slot fills in Tracks 1 or 2. If a Track 3 strategy looks promising and operator wants to elevate it to a real slot, it must be re-tested under Tracks 1/2 rules: three-filter methodology selection, full pre-registration, validation gate passage, then paper-forward in real time. Walk-forward verification on historical data alone does NOT count as paper-forward verification. (4) Track 3 cannot consume autonomous loop resources. The cron runs Tracks 1 and 2 work (monitor cleared strategies, regenerate dashboard, paper-forward detector on cleared watchlists). Track 3 work happens exclusively in supervised CC sessions initiated by operator. (5) Track 3 results are NOT cited as evidence in any Track 1 or Track 2 decision_log entry. Walk-forward results are research observations, not directive evidence. **What Track 3 is NOT.** Track 3 is not a backdoor for adding strategies that would fail Track 2's qualification criteria. It is not a way to call held-out backtest results 'paper-forward.' It is not a way to bypass the three-filter methodology. Anyone reading this entry should understand that Track 3 exists to test a specific operator-stated hypothesis under explicitly-labeled research conditions, and the boundary rules above are what prevent it from becoming the survivorship-bias-amplifying mechanism the original framework directive was designed to prevent.
- **rationale:** Operator stated in this session that they hold a hypothesis about whether held-out historical-window backtests predict forward performance. Testing the hypothesis is legitimate research. Pretending the test is 'paper-forward' would be dishonest -- the held-out data already exists and the strategy selection is influenced by knowledge of that data. The boundary rules in this entry are the discipline that lets the test happen honestly: clear labeling, separate storage, no auto-promotion, no consumption of disciplined-track resources. Without those boundaries, Track 3 would slowly contaminate Tracks 1 and 2 by ambiguity (operator's brain treats 'experimental win' the same as 'real win' if labels are weak). With the boundaries, Track 3 is a useful research instrument that informs operator's intuition without compromising the disciplined tracks' integrity.
- **alternatives_considered:**
  - Don't add Track 3; stay disciplined within Tracks 1+2 only (rejected -- operator wants to test the hypothesis; refusing to scaffold the test means operator either runs it informally without audit-chain integration or doesn't run it at all, both worse than scaffolded test with boundaries)
  - Add Track 3 but label its results 'paper-forward' (rejected -- that's the dishonest naming the framework directive was built to prevent; held-out historical-window backtests are NOT paper-forward, conflating them undermines both terms)
  - Add Track 3 results to `strategy_register.md` (rejected -- mixes Track 3 observations with Track 1/2 verdicts in the canonical strategy record; future audits would lose the ability to distinguish methodology-cleared strategies from research-track candidates)
  - Allow Track 3 strategies to auto-promote to Track 1/2 slots if they perform well on held-out data (rejected -- defeats the purpose of having three tracks; walk-forward verification != paper-forward verification; re-testing under Track 1/2 rules is the only way to elevate)
  - Allow autonomous loop to run Track 3 (rejected -- autonomous loop runs every 15 min; Track 3 research benefits from supervised attention not autonomous churn; resource isolation also prevents Track 3 from consuming the loop's compute and audit chain bandwidth)
- **audit_finding_refs:** `entry 32 (this commit chain)`
### Track 1 restart with explicit acceptance of recovery-spawn architecture gap -- workers do not exit cleanly after SKILL completion, watchdog kill misses targets, recovery agents run status-only; failure mode will recur

- **ts_utc:** `2026-05-25T08:00:00+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Restart Track 1 after 2026-05-24T09:36Z stoppage with explicit acceptance that the recovery-spawn architecture gap (entries 26/28/29) was the root cause and remains unfixed. Today's investigation (this session, steps 1-11 reported) confirmed three operational bugs working together: (Bug A) workers spawned by MCP scheduled-tasks with --input-format stream-json do not exit after completing SKILL.md work; PIDs 24256/24488/16128 from yesterday's three fires all still alive 24h+ later. (Bug B) PID 16128 (the 09:28:36Z fire matching MCP lastRunAt) reached SKILL preamble and touched sentinel once at 09:29:05Z via entry-29 patch's --mark-done call, then stopped without producing a sprint commit. Cause of mid-SKILL stop not investigated. (Bug C) Watchdog stale-cleanup 'terminating 1 claude.exe + descendants' ran 19 times during the loop but never killed PIDs 24256/24488/16128 -- kill target appears to be wrong process tree (workers parented by Claude Desktop, not the spawned recovery agents). (Bug D) Recovery agents spawned by watchdog ran status/verification only with stdout_bytes=0 each time, never running the actual sprint, so sentinel stayed stale and the watchdog loop continued for 19 iterations until the final recovery agent created cron_paused.flag at 12:52Z, halting the loop per entry 31 Option D failure remedy. Acceptance: Track 1 will be restarted with cleanup of yesterday's zombie workers and lock file, sentinel touch, watchdog state reset, flag removal. The architecture gap remains unfixed. Track 1 will likely re-enter failure mode after some number of fires (yesterday: 3 fires before stop). When it re-enters failure mode, the cron_paused.flag will be re-created by recovery agent per entry 31 playbook, and Track 1 will be paused again. Operator will run cleanup-and-restart per this entry's playbook each cycle, or eventually invest in fixing Bug A (root cause). Track 1 operates as 'supervised bursts with manual restarts,' not 'set-and-forget autonomous monitoring.' Tracking-flag item for architecture fix remains open and is now load-bearing on Track 1's reliability.
- **rationale:** Architecture fix requires Claude Code source-code investigation (Bug A -- worker exit behavior under stream-json input mode), watchdog kill-target redesign (Bug C -- process tree identification), and recovery agent redesign (Bug D -- session-resume agents must run the sprint, not just status/verification). Each of these is a substantial investigation/build effort. Today's path: accept the gap, restart Track 1 in supervised-bursts mode, focus session energy on Track 2 work (which advances the 75% coverage goal regardless of Track 1 state). The operational cost of supervised-bursts mode is periodic manual restart per cycle, which is acceptable until a dedicated architecture-fix session is scheduled.
- **alternatives_considered:**
  - Investigate and fix Bug A before restart (deferred -- investigation could be long, blocking Track 2 work; Track 2 doesn't need Track 1)
  - Leave Track 1 paused indefinitely (rejected -- operator wants the paper-forward signals for cleared Divergence positions, even if intermittent)
  - Restart without cleanup (rejected -- zombies + stale sentinel would trigger loop within minutes; net effect would be wasted CC effort and immediate re-pause)
  - Restart with just zombie cleanup but skip watchdog state reset (rejected -- watchdog state.json mode=monitoring_paused would short-circuit watchdog activity; proper reset required for clean operation)
- **audit_finding_refs:** `entry 26 (recovery-spawn architecture gap)`, `entry 28 (orphan-spawn correction)`, `entry 29 (SKILL.md patch + verification mechanism gap)`, `entry 31 (Option D verification with explicit acceptance of recovery-spawn gap)`, `entry 32 (framework amendment)`
### Maintenance fire 2026-05-26 02:27Z after ~17h Track 1 gap -- sentinel touched, paper-forward detector idempotent (0/0), dashboard regen; coverage/PHASE-1-TRACKER flagged STALE vs Track 2 amendment

- **ts_utc:** `2026-05-26T02:30:00+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Executed one routine sprint fire as a MAINTENANCE fire (no new strategy enrolled, no validation run). Steps completed: (1) last_sprint_fire.txt touched 2026-05-26T02:26:54Z, resetting the 4h-quiet watchdog after a ~17h gap since the prior touch 2026-05-25T09:02:20Z -- consistent with the documented supervised-bursts failure mode (Track 1 restart entry, 2026-05-25T08:00Z). (2) paper_forward_full_universe.py ran idempotently: batch 250/1107 (offset 639->889), 0 new entries, 0 exits, 12 open positions (cap-saturated), 37 history, 119 fetch errors (yfinance long-tail delisted/renamed misses + US market closed at 22:26 EDT -- no new daily bar). (3) dashboard.html regenerated (1,043,030 bytes). No new strategy enrolled and no validation run: the staged queue is empty and new-slot work requires three-filter selection plus a frozen config_hash BEFORE data, which cannot be honestly compressed into a single ~12-min fire. FLAG FOR OPERATOR RECONCILIATION: winners_assignment.json (generated 2026-05-22) still routes paper-forward through FOUR cleared_sources (divergence/mbv/pmr/str US = 1,107 tickers), but the Track 2 framework amendment (entry 2026-05-24T09:00Z, commit 7f05546) grandfathers ONLY divergence into US slot 1; mbv/pmr/str are Pre-Framework and do NOT fill slots or count toward the 75% per-market coverage target until re-validated under the amended gate via Method A (fresh OOS) or Method B (post-2026-05-22 forward data). Therefore the ceo_brain.md PHASE 1 TRACKER (US coverage 1,107/1,621 = 68.3%, score 6/10) is STALE under the amended framework -- the legitimate amendment-grandfathered coverage is divergence-only until re-validation lands. The 807-ticker routing change (mbv 323 + pmr 232 + str 252) was NOT applied this fire: stripping those tickers from paper-forward is a deliberate re-validation-sprint decision for the operator, not a maintenance-fire action, and the amendment says Pre-Framework strategies MAY be re-tested, not that they must be immediately purged from the existing paper-forward roster.
- **rationale:** A routine fire after a multi-hour Track 1 stoppage should restore the watchdog sentinel and run the value-producing idempotent detector (operator wants paper-forward signals for cleared positions even when intermittent, per the Track 1 restart entry) without manufacturing strategy progress that pre-registration discipline forbids in a single fire. The most useful honest output of this fire is surfacing the coverage/tracker staleness introduced by the Track 2 amendment so the operator can schedule the Method A/B re-validation sprint that the amendment requires, rather than letting the dashboard and brain keep asserting a 1,107-ticker union coverage number that the amended framework no longer recognizes.
- **alternatives_considered:**
  - Rewrite winners_assignment.json to divergence-only now (rejected -- an ~807-ticker paper-forward routing change is a re-validation-sprint decision for the operator, not a maintenance-fire action; amendment Change 1 says Pre-Framework strategies MAY be re-tested, not that they must be purged immediately)
  - Enroll a new strategy / select a new slot this fire (rejected -- three-filter methodology selection + frozen config_hash-before-data cannot be honestly done in one ~12-min fire; doing it fast would violate the pre-registration discipline the framework exists to protect)
  - Skip the detector to avoid Telegram noise (rejected -- detector is idempotent and produced 0 entries/0 exits with market closed; running it keeps state and dashboard current with no spurious alerts)
  - Edit ceo_brain.md PHASE 1 TRACKER numbers directly this fire (deferred -- the corrected coverage depends on the re-validation outcomes which have not been run; flagging the staleness in the append-only decision log is the honest record without fabricating new numbers)
- **audit_finding_refs:** `entry 32 (2026-05-24 framework amendment -- per-market caps, divergence grandfathered)`, `entry 35 (2026-05-25 Track 1 restart, supervised-bursts mode)`
### Sprint iter 4/5 yfinance flap halt

- **ts_utc:** `2026-05-26T14:07:50+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Aborted iteration 5 of catch-up burst after iters 2/3/4 produced 9/12/11 entries and 6/9/9 exits on overlapping tickers (AA, AMBA, IFNNY, SYNA, TTDKY flipping in/out each run) with no new daily bar in between.
- **rationale:** Catch-up assumes each missed fire had distinct market data, but 16 missed fires all landed mid-day on the same trading session. Re-running paper_forward_divergence against intraday yfinance polls produced spurious churn inflating signals_log + paper_forward_positions.json by ~32 fake transitions. Iter 5 would extend churn. Time budget (~49 min elapsed, 50-min cap) also effectively exhausted.
- **alternatives_considered:**
  - Complete iter 5 per SKILL letter
  - Roll back iter 3+4 state churn
  - Switch detector to gate on new daily bar before processing
### Recovery 2026-05-27 03:00Z: watchdog false-loop broken, orphan-spawn gap flagged for manual intervention

- **ts_utc:** `2026-05-27T03:03:42+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Headless recovery session (watchdog spawn, lock 02:55:15Z) verified state per session_resume_prompt and took the minimal safe corrective action: marked the sentinel fresh to break the watchdog false-recovery loop, committed, and pushed. Deliberately SKIPPED the full-universe paper-forward scan this turn (staged queue empty; scan is the most likely P-001 hang site and re-running it in a timeout-bounded headless turn risks reproducing the hang that is accumulating orphans). Next real cron fire or a supervised manual fire should run the scan.
- **rationale:** State verification found the recovery mechanism itself has been failing since 2026-05-26T13:17Z (cc_watchdog_state.json last_recovery_ts), recoveries_today=0 despite mode=recovering and 5 spawn attempts 02:11-02:55Z with sentinel-staleness growing 33->77min. 16 claude.exe processes are alive = accumulated hung recovery/sprint spawns (the known unsolved 'in-flight orphans' architecture gap from 2026-05-24 decision_log). These orphans likely hold REPL_BUSY locks, which is why fresh spawns do no productive work. The 02:55Z crash-detect Telegram FAILED to send (ok:false, msg_id null) - first send failure observed. Last productive committed fire was the 00:49Z STR/US reassignment, recovered+pushed as 869af80 at 01:27Z; the 01:38Z sentinel touch produced no commit. No substantive work lost. Orphan cleanup requires killing other claude.exe PIDs - hard to reverse, not authorized for an unattended headless turn, and the system is one step from manual-mode per CRASH_RECOVERY.md section 9; flagged for Ahmed.
- **alternatives_considered:**
  - Run the full paper-forward scan + dashboard regen (rejected this turn - highest hang/timeout risk, likely the orphan-producing step)
  - Kill the 16 orphaned claude.exe processes to clear REPL_BUSY (rejected - destructive, unauthorized for unattended headless session, risk of killing legitimate sessions; left for operator)
  - Do nothing / report only (rejected - would leave the watchdog screaming every ~11min with growing staleness)
### Recovery 2026-05-27 04:05Z: watchdog runaway HALTED via cron_paused.flag (manual mode); orphan root-cause isolated

- **ts_utc:** `2026-05-27T04:05:00+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Session-resume recovery (PID 31588, the 07:58 UAE watchdog spawn) verified state and created `scripts/cron_paused.flag`, halting the watchdog at its intentional-pause check (evaluated before the lock/spawn logic) so no further recovery spawns occur. Did NOT kill orphans (destructive, risks a live Desktop/foreground session; left for operator). Committed + pushed.
- **rationale:** The orphan-spawn gap the 03:03Z entry flagged is now isolated to TWO compounding bugs: (1) `Get-ClaudeCodeCliProcesses` (v1.3 cmdline filter `claude-code|\@anthropic-ai\claude-code\`) matches only 4 of 17 live claude.exe — 9 are readable-but-non-matching and 4 have null/unreadable CommandLine, so pre-spawn `Stop-StaleClaudeSessions` spares 13 hung orphans that keep holding REPL_BUSY; (2) `cc_watchdog_state.json` is MISSING, so `recoveries_today` reloads as 0 every tick and the 5/UTC-day cascade brake (added after the 2026-05-22 49-orphan incident) never trips -> unbounded respawn. crash_log.json shows 0 completed recoveries today (all hang, neither OK nor FAILED logged) while detect-Telegrams fired 07:36/47/58. last_sprint_fire.txt frozen at 03:03Z; last productive committed fire remains the 00:49Z STR/US reassignment (869af80, 01:27Z). No NEW substantive work lost since 03:03Z — recoveries produced no commits, only orphans. Remote (origin/main) == local at a83d88e; nothing unpushed.
- **alternatives_considered:**
  - Kill the 13 orphan claude.exe now (rejected - destructive, may include a live foreground/Desktop session; unauthorized for an unattended recovery turn; pause flag stops growth so the kill is no longer time-critical)
  - Touch sentinel again like the 03:03Z fire (rejected - a false 'sprint fired' signal that only delays the loop ~30min; the pause flag is the honest designed mechanism and stops it for good)
  - Leave running and only report (rejected - orphan pile actively growing ~1 per watchdog cycle)
### Retire 3x/10x annual mandate — replace with Kelly-bound realistic anchor (5-10% CAGR)

- **ts_utc:** `2026-06-11T15:56:34+00:00`
- **methodology_source:** `framework_directive`
- **decision:** Retire the 3x/10x annual return mandate (hard minimum 200%, aspirational 1,000%) as a project target. Replace with a constraint-physics-realistic anchor: portfolio compounding at 5-10% CAGR with sub-1% drawdown, where per-strategy edge is measured by the validation gate and portfolio output is the sized aggregation of cleared strategies. The 5-10% figure is an ANCHOR measured toward, NOT a target chased — the moment it becomes a hard target, the phantom-mandate distortion returns. No mandate above the Kelly bound is accepted.
- **rationale:** Per Kelly-bound derivation: CAGR ≤ (Sharpe²/2) × (risk_used / optimal_Kelly_risk). For AIG's locked 1.5% per-trade risk cap (Rule 8) against a full-Kelly optimum of ~20-30% for daily-frequency strategies, the used fraction is ~5-7.5% of optimal Kelly. This yields a ceiling band of approximately 5-10% CAGR, with 15% requiring a Sharpe of ~2.0 (world-class, near-impossible in retail halal long-only no-leverage markets). The 3x/10x mandate (200-1000% CAGR) would require Sharpe ratios that do not exist in this constraint box. Continued operation under the phantom mandate produces goalpost-relocation pressure on every Track 2 candidate selection. Honest retirement is consistent with the framework directive's Brutal Honesty principle. An external three-model review (ChatGPT, Gemini, DeepSeek, 2026-05-29) independently converged on retiring the mandate with high confidence; that convergence corroborates the derivation but the derivation, not the model count, is the proof.
- **alternatives_considered:**
  - Keep the mandate as aspirational only (rejected — even as aspiration it distorts candidate selection; the math forbids it, so it isn't aspiration, it's fiction)
  - Lower the mandate to a still-ambitious number like 50% (rejected — 50% CAGR requires Sharpe well above 2.0 under the 1.5% cap; same category of impossibility)
  - Drop return targets entirely, pure research lab (deferred — viable framing but operator chose a stacked hybrid where Path 1 output retains a measured anchor)
- **audit_finding_refs:** `entry 8 (Phase 1 cap and slot structure)`, `entry 32 (framework amendment)`, `External: AIG Forensic Council Verdict v2 (2026-05-29, external document, not an AIG decision_log entry)`
### Archive v7 Scope Document as historical-aspirational reference

- **ts_utc:** `2026-06-11T15:57:32+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Archive the v7 Scope Document (27 agents, 8 layers, CEO Fully Autonomous, hybrid Code+Cowork, 4-week launch checklist) as a historical-aspirational reference. It no longer describes operating reality. The current authoritative operating specification is: the framework directive (2026-05-22, commit 33d93ae) + the entry 32 amendment (per-market caps, TradingView source criteria) + Tracks 1/2/3 per entry 33 + the Kelly-bound realistic anchor per entry 39. Mark the v7 Scope file header with an "ARCHIVED — superseded, see decision_log entry 40" banner; do not delete it (evidence preservation).
- **rationale:** The v7 Scope describes an architecture (27 autonomous agents) and a mandate (3x/10x) the project moved away from months ago and formally retired in entry 39. Leaving it un-archived means future sessions reading the scope inherit a phantom architecture and a phantom mandate. Archiving with an explicit banner preserves the historical record while preventing it from being mistaken for current spec.
- **alternatives_considered:**
  - Delete the v7 Scope (rejected — evidence preservation; the project keeps superseded artifacts with banners, never deletes)
  - Leave it unmarked (rejected — risks future sessions treating it as current)
- **audit_finding_refs:** `entry 39 (mandate retirement, same chain)`, `entry 32 (framework amendment)`, `entry 33 (three tracks)`
### Phase 2 verdict: recovery-spawn gap root-caused and fixed (watchdog v1.5); Track 1 driver decision open

- **ts_utc:** `2026-06-11T15:58:45+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** The recovery-spawn architecture gap (entries 26/28/33, halted by entry 38's cron_paused.flag) is FIXABLE and is now fixed at the watchdog layer: cc_watchdog.ps1 v1.5 (commit d21dee6, 2026-06-11) repairs four compounding bugs. The 13 orphans named in the flag's checklist no longer exist — the host was rebooted between 2026-05-27 and 2026-06-11 and a live-process audit found zero true orphans. cc_watchdog_state.json is restored and persisting, so the 5/day cascade brake works. The remaining OPEN item is the Track 1 driver: the aig-mode1-sprint Cloud Routine NO LONGER EXISTS (routine list returned empty on 2026-06-11), and the watchdog recovery path is status-only by design (Bug D: session_resume_prompt.txt verifies five items and reports; it never instructs sprint work), so as of this entry NOTHING fires Track 1 sprints. Operator must choose: (a) recreate the scheduled cloud routine, (b) promote the watchdog recovery path to a real sprint driver (requires replacing the status-only resume prompt), or (c) keep Track 1 retired. cron_paused.flag STAYS UP until that decision. NUMBERING NOTE: the work-block plan reserved position 41 for a 'Tareq reservation' entry and 42 for this verdict, but the plan message was truncated in transit before entry 41's text; this verdict therefore occupies position 41 and the Tareq entry will follow out of planned order when the operator re-sends it.
- **rationale:** The four v1.5 bugs, each verified against the live process table: (1) the v1.3 CommandLine filter ('claude-code' / npm path) matched NOTHING after the CLI migrated from npm to the native installer (~\.local\bin\claude.exe) and silently spared processes with unreadable command lines — 13 of 17 orphans escaped the 2026-05-27 pre-spawn reap exactly this way; (2) $ClaudeExe resolved only the two deleted npm paths, so every real recovery spawn would have failed outright; (3) the bare 'claude-code' substring, if widened naively, reaps Claude Desktop's local-agent-mode sessions (Roaming\Claude\claude-code\<ver>\claude.exe — live remote-control sessions run there); v1.5 instead classifies by ExecutablePath, spares the --remote-control listener, matches headless -p/--print spawns, and for bare/unreadable command lines reaps only when the parent process is dead (true orphan); (4) 'return ,$cli' handed callers a nested array so @(...).Count was 1 at every call site regardless of matches — the same class of bug v1.4 fixed in Get-ProcessDescendants. Verification: 5/5 regex spot-checks passed; the filter returned 0 candidates against the live table (Desktop, helpers, remote-control listener, 4 interactive tabs, 2 Desktop agent sessions all spared) and caught a synthetic dead-parent orphan exactly. The cloud routine's disappearance also explains the 2026-05-27 10:35-11:25 sprint burst firing AFTER the 04:05Z flag — the routine was a trigger path independent of the watchdog flag; at some point since, it was removed entirely (by whom is not recorded).
- **alternatives_considered:**
  - Delete cron_paused.flag now and let the watchdog drive Track 1 (rejected — Bug D means recoveries do no sprint work; with a 15-day-stale sentinel the watchdog would burn its daily cap on status-only spawns)
  - Recreate the aig-mode1-sprint cloud routine unilaterally (rejected — restarting the sprint engine is an operator decision with API cost and Track 2/3 implications)
  - Leave the flag up with no recorded decision (rejected — Track 1 would be silently dead; this entry records the state and forces the explicit choice)
- **audit_finding_refs:** `entry 26 (orphan-spawn architecture gap)`, `entry 28 (cron re-enable accepting the gap)`, `entry 33 (Track 1 restart; recovery agents run status-only)`, `entry 38 (runaway halt; root-cause isolation)`, `commits d21dee6 (watchdog v1.5), 2e293f7/930835d (Step 0.4 tree), dbdc027 (entry 38 json sync), 658ed8c (entry 39), 2d94f5c (entry 40)`
### Reserve Tareq pure-price-action system as Phase B UAE candidate (lightweight hypothesis capture, not pre-registration)

- **ts_utc:** `2026-06-11T16:08:55+00:00`
- **methodology_source:** `three_filter_methodology`
- **decision:** Reserve "Tareq pure-PA" as Phase B UAE slot candidate; full canonical pre-registration (spec_hash freeze) happens at Phase B open, NOT now. Hypothesis: expert uses zero indicators (no RSI/MACD/Fib/EMA) per 2026-05-05 Whisper analysis of 18 videos (support/دعم 81, trend 61, bottom/قاع 49). Filter-1 archetype: pure price action — support/resistance + trendlines + liquidity, zero indicators. This timestamps the insight (anti-contamination: hypothesis predates any test) without freezing a spec months before testing. No spec_hash, no TRIAL_BUDGET row, no config.py change.
- **rationale:** spec_hash value = adjacency to testing (freeze then test, no tuning between). Freezing now, months before Phase B, locks out Phase-A learnings. β decision (separate pure-PA system, not V7 redesign) is the falsifiable choice: same three filters + gate as any candidate; clears = validated, fails = intuition doesn't codify — both are evidence. Tareq transcript is hypothesis SOURCE, not validated edge; no special treatment.
- **alternatives_considered:**
  - α V7 redesign (rejected — breaks working system, no head-to-head)
  - Full pre-registration now (rejected — premature freeze)
  - γ defer without capture (rejected — finding rots, timestamp lost)
- **audit_finding_refs:** `entry 33 (tracks)`, `entry 32 (UAE cap 4)`
### Track 1 driver change (Option A2, Task Scheduler + headless claude -p): first supervised fire FAILED on headless/background mismatch; fixes F1/F2/F3 applied

- **ts_utc:** `2026-06-11T22:01:30+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** The Track 1 driver is being changed, not restored: the original aig-mode1-sprint registration was lost in a platform split (cloud routines: 1-hour minimum, no local filesystem; Desktop-local tasks: registration gone, requires Desktop app open). Option A2 chosen — Windows Task Scheduler task AIG-Mode1-Sprint (daily 00:12 anchor + PT15M repetition = 12,27,42,57, RandomDelay PT347S, MultipleInstances=IgnoreNew, ExecutionTimeLimit 75 min, workdir=repo root) running scripts/sprint_task_launcher.ps1 -> headless `claude -p` at the user-scoped SKILL. A2 over A1 (Desktop task) for durability: reboot-safe, no Desktop-open dependency; structural Bug-A avoidance (-p exits when the turn ends, by design); v1.5 reaper covers any hang. A1 rejected for the Desktop-open dependency; A3 (in-session cron) rejected for 7-day auto-expiry. First supervised fire (2026-06-11 17:12Z slot) FAILED criteria (c) no sprint commit and (f) watchdog stale-check (reached 2/3 before the recreated flag stopped the chain at the would-be confirm tick; no recovery spawned). Fixes applied before re-fire: F1 HEADLESS CONTRACT in SKILL.md (never background, never end turn to wait, all steps in one turn); F2 launcher preamble stating the same; F3 per-step `sprint_catchup.py --mark-done` after each SKILL step (verified timestamp-only: it writes the sentinel and returns, no catch-up accounting touched). SentinelStaleSecondsSoft NOT raised. Entry 44 reserved for a successful restoration fire.
- **rationale:** Failed-fire root cause (worker transcript 94c97cce): the worker hit yfinance flaps (EXAS 404/delisted), launched the detector retry as a BACKGROUND task, and ended its turn 'waiting' — in headless -p, ending the turn exits the process and background notifications never arrive, so SKILL steps 3-5 (queue drain, dashboard, commit+push) were unreachable; exit code 0 because the turn ended normally. What PASSED and stands as evidence for A2: spawn on the exact :12 slot (PID 22860, 17:12:01Z, parent=launcher, native CLI exe); T2 auth independent of Desktop (Task Scheduler probe with svchost ancestry returned AUTH_OK via CLI credentials); IgnoreNew proven (the 17:27 slot skipped while the 19-min worker ran); clean exit=0 with ZERO leftover CLI processes (structural anti-zombie claim confirmed); watchdog v1.5 conduct correct throughout (left the live worker alone, accurate claude_alive=False only after actual exit, flag honored at the next tick). The secondary failure mode — 19-min single iteration with one sentinel mark outran the 15-min soft threshold AFTER the worker exited — is addressed by F3's per-step marks rather than threshold relaxation, preserving detection sensitivity.
- **alternatives_considered:**
  - A1 Desktop-local scheduled task (rejected — fires only while the Desktop app is open; app close/crash silently kills Track 1)
  - A3 in-session durable cron (rejected — documented 7-day auto-expiry; dies with the hosting session)
  - Raise SentinelStaleSecondsSoft to cover long iterations (rejected — degrades crash-detection sensitivity; per-step sentinel marks achieve liveness without it)
  - Retry the fire without fixes (rejected — same background+turn-end mismatch would recur deterministically under yfinance flaps)
- **audit_finding_refs:** `decision_log position 41 (Phase 2 verdict; driver options framed)`, `entry 27 (per-iteration --mark-done discipline)`, `entry 33 (recovery agents status-only)`, `commits d21dee6 (watchdog v1.5), 3cd3a34 (launcher), 112e055 (failed-fire runtime state)`
### Track 1 driver RESTORED (Option A2) — restoration fire passed all 8 supervised criteria; Track 1 LIVE

- **ts_utc:** `2026-06-11T22:50:28+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Track 1 is LIVE on the Option A2 driver (entry 43): Windows scheduled task AIG-Mode1-Sprint -> scripts/sprint_task_launcher.ps1 -> headless `claude -p` at the user-scoped SKILL, cron-equivalent 12,27,42,57 * * * * with PT347S jitter, IgnoreNew, 75-min limit. cron_paused.flag removed 2026-06-11T22:01:48Z (renamed .removed-20260611T220148Z). The 2026-06-11 22:12Z-slot restoration fire passed all 8 supervised criteria with fixes F1 (SKILL headless contract) + F2 (launcher preamble) + F3 (per-step sentinel marks; --mark-done verified timestamp-only) in force.
- **rationale:** Full evidence, worker PID 25384, transcript f20c010e: (a) spawn 22:14:26Z (22:12 slot + 2m26s jitter), parent=launcher powershell, exe=~\.local\bin\claude.exe; (b) sentinel marked per step, six advances 22:15:50 -> 22:35:42 -> 22:43:42 -> 22:43:51 -> 22:44:12 -> 22:45:06Z — the 20-min detector step stayed under the 30-min hard threshold with the worker alive, so the watchdog had nothing to fire on (the 15-min soft branch requires process-absent); (c) sprint commit 5f4152f landed AND pushed (steps 1-5 all ran in one turn: catch-up 0 missed, detector 1030 watched / 23 entries / 17 exits / 33 open / 166 history under yfinance flaps handled FOREGROUND, queue empty WCK finalized with no auto-enrollment per directive, dashboard regenerated, commit+push via commit_session.ps1); (d) Telegram sent (start notification + entry/exit alerts, log updated 22:43:15Z); (e) worker exited exit=0 at 22:47:37Z, 33m11s spawn-to-exit, zero leftover CLI processes; (f) watchdog quiet throughout and after: mode=normal, consecutive_stale_checks=0, recoveries_today=0, no recovery spawned; (g) steps 3-5 demonstrably ran (commit content + 1.1MB dashboard regen); (h) transcript shows ZERO voluntary background launches — the harness auto-backgrounded the one long detector command and the worker foreground-waited on it inside the turn instead of ending the turn, exactly the F1/F2 contract. IgnoreNew additionally skipped the 22:27 and 22:42 slots while the worker ran, then normal cadence resumes. The worker itself committed runtime state + HANDOFF (29f0f0a), demonstrating the headless push path (commit_session.ps1 GH_TOKEN) works.
- **alternatives_considered:**
  - Hold Track 1 paused pending more supervised fires (rejected — all 8 criteria passed on a fire that exercised the worst known stressor, a 20-min yfinance-flap detector step; the watchdog + IgnoreNew + launcher timeout provide layered containment for future anomalies)
  - Declare LIVE after the first (failed) fire with manual patching (rejected 2026-06-11 — fail-fast protocol held: flag up, root cause, fixes, re-fire)
- **audit_finding_refs:** `entry 43 (driver change, failed first fire, fixes F1/F2/F3)`, `decision_log position 41 (Phase 2 verdict)`, `commits 5f4152f (restoration sprint fire), 29f0f0a (worker runtime state), 3cd3a34 (launcher), d21dee6 (watchdog v1.5)`
### Pre-register TSM-12 (trial 40, tsm12_us_1d) — first Phase 1 three-filter candidate, US slot 2, spec_hash efe8ac7b47f10a0f

- **ts_utc:** `2026-06-12T07:25:45+00:00`
- **methodology_source:** `three_filter_methodology`
- **decision:** TSM-12 (time-series momentum, 12-month/252-trading-day total return, monthly last-trading-day rebalance, long-if-positive else flat, equal weight, long-only, no stop, no leverage) is pre-registered as trial 40 (tsm12_us_1d, US-only) with spec_hash efe8ac7b47f10a0f, frozen at operator approval 2026-06-12 BEFORE any test data was evaluated. Universe frozen at 1,603 tickers (universe/us_halal_full.txt, universe_sha256 6dfca4bd...775e4e bound inside the spec_hash). n_trials_registered bumped 39 -> 40 in the same atomic commit as the trial-budget row and the canonical spec text (ad075d0); tests 9/9 green at commit. Engine implementation of the frozen spec follows pre-registration and precedes the staged_validate run (operator guardrails: spec-exact code, zero parameters beyond spec, edge-case conventions inherited from trials 1-39 and documented in the verdict entry).
- **rationale:** Three filters applied: F1 archetype diversity — cleared US strategies (Divergence, MBV, PMR, STR) are pullback/mean-reversion family; trend_following is priority 1 in the register's Filter-1 order and slot 2 has no cleared trend strategy. F2 evidence tier — T1: Moskowitz/Ooi/Pedersen 2012 is the register's named T1 trend-following exemplar (peer-reviewed, multi-market, multi-decade). F3 readiness — daily bars suffice, long-only (Rule 15), no leverage (Rule 16), halal universe; no disqualifiers. The spec_hash derivation convention is newly defined in the register (first 16 hex of SHA-256 over the canonical param string, which embeds the universe content hash) because the 2026-05-24 session's convention and its draft hash 10d76bc7e0ee5e28 were lost with that session — the old hash had already been invalidated by a 1,621-vs-1,603 universe-count mismatch; the new derivation binds universe CONTENT so any drift invalidates the hash by construction. Live count verified 1,603 (zero duplicates) immediately before freeze.
- **alternatives_considered:**
  - Re-derive or adopt the lost 2026-05-24 hash convention (impossible — exists nowhere on disk; transcripts searched)
  - Enroll the standard 3 markets like trials 1-39 (rejected — approved spec is US slot 2 only; trial budget extends by exactly 1)
  - Cross-sectional momentum variant (rejected — spec is time-series per the T1 source; cross-sectional is a different claim and a different future candidate)
- **audit_finding_refs:** `entry 8 (Phase 1 cap and slot structure)`, `entry 32 (framework amendment)`, `entry 39 (Kelly-bound anchor)`, `commit ad075d0 (atomic pre-registration)`
### TSM-12 verdict (trial 40, tsm12_us_1d): PORTFOLIO_FAIL — US slot 2 remains open

- **ts_utc:** `2026-06-12T17:33:35+00:00`
- **methodology_source:** `three_filter_methodology`
- **decision:** TSM-12 (trial 40, tsm12_us_1d, spec_hash efe8ac7b47f10a0f) FAILED the validation gate at portfolio level — honest FAIL, recorded as-is: verdict PORTFOLIO_FAIL at 2026-06-12T08:15:35Z (validation_tsm12_us_1d.json, staged run, 9 batches, live data). Full numbers: 3,473 portfolio trades from 1,008 contributing tickers; portfolio expectancy 1.0537; win rate 0.2989 < 0.40 floor; Sharpe raw 0.2841, deflated -0.3949 < 0.5 under the n=40 registered-trials haircut (n_trials_registered per entry 45); 95% CI on mean trade [-0.008486, +0.018225] — lower bound not > 0. Three independent gate criteria failed. US slot 2 remains OPEN. Next candidate proceeds per Filter 1 priority order at the next Track 2 session (Session A2). NO re-run, NO parameter tuning, NO gate adjustment for this trial — the spec was frozen before data (entry 45) and the result stands. UNIVERSE CONVENTION (documented per the pre-registration guardrail in entry 45): the spec froze 1,603 tickers (universe/us_halal_full.txt, universe_sha256 bound inside spec_hash); the engine evaluated 1,122. The 481 exclusions follow the trials 1-39 pre-backtest data-integrity convention, applied per ticker BEFORE any backtest: (a) DATA_ERROR, 63 tickers — yfinance fetch raised or returned an empty frame (delisted/renamed names); the engine never falls back to synthetic data on a real run, the ticker is skipped; (b) BLOCKED_DATA, 418 tickers — integrity_check in aig/data.py blocked the ticker: 394 unadjusted split/spike suspected (any single-bar |return| > 50%), 23 insufficient history (< 300 daily rows), 2 high/low/close inconsistency (the gate also checks NaN OHLC, non-positive closes, non-monotonic dates, and calendar gaps > 10 days for US; zero hits this run). Excluded tickers do not enter portfolio aggregation. The per-ticker deflation haircut still used the FULL frozen universe (n=1,603 trials, visible in per-ticker reasons); only the portfolio-level deflated Sharpe used the n=40 registered-strategies haircut.
- **rationale:** The gate exists to make exactly this outcome cheap to accept: spec frozen at entry 45 before any data, engine implemented spec-exact (commit 90db17f, 14/14 tests), one shot, result recorded. Expectancy above 1.0 and a positive raw Sharpe were not enough — the deflated Sharpe is negative under the trial-count haircut and the CI straddles zero, so the measured edge is indistinguishable from selection noise across 40 registered trials. OBSERVATION ONLY, NO ACTION: trend-following as an archetype is structurally a low-win-rate / high-payoff-asymmetry family (cut losers monthly, ride winners), so the 0.40 portfolio win-rate floor may exclude the entire trend-following archetype regardless of whether it carries real edge — TSM-12's 0.299 is in the normal range for the family per its T1 literature. This is flagged for a future framework-level discussion through the proper amendment process (entry 32 precedent); the gate is UNCHANGED for this trial and all future trials unless and until such an amendment lands. Changing the floor now, after seeing this result, would be the post-hoc contamination the pre-registration discipline forbids. Separately, the 394 split/spike data blocks are a data-quality finding about the yfinance cache layer worth investigating on infrastructure time; any re-test after data fixes would be a NEW trial with its own pre-registration and budget row.
- **alternatives_considered:**
  - Re-run with a relaxed or archetype-conditional win-rate floor (rejected — post-hoc gate change after seeing the result is the exact contamination pre-registration exists to prevent; floor change requires a framework amendment decided on framework time, not trial time)
  - Re-run on the 1,122 data-surviving tickers as a 'clean universe' (rejected — the universe was frozen at 1,603 inside spec_hash efe8ac7b47f10a0f; a filtered universe is a different spec, hence a new trial and a new budget row)
  - Fix the unadjusted-split data blocks first and re-validate (deferred — legitimate infrastructure work, but the re-test would be a new trial; recorded as an observation, not an action of this entry)
  - Count the verdict as PASS on expectancy 1.0537 alone (rejected — the gate is conjunctive by design; deflated Sharpe and CI criteria failed)
- **audit_finding_refs:** `entry 45 (TSM-12 pre-registration, trial 40, spec_hash efe8ac7b47f10a0f)`, `entry 8 (Phase 1 cap and slot structure)`, `entry 32 (framework amendment / amendment-process precedent)`, `entry 39 (Kelly-bound anchor)`, `commits ad075d0 (atomic pre-registration), 90db17f (spec-exact engine implementation)`, `validation_tsm12_us_1d.json (provenance utc 2026-06-12T08:15:35+00:00)`
### Amendment 1 adjudication: provenance ratified, provision superseded as dead letter; directive file recovered; config knobs removed

- **ts_utc:** `2026-06-12T19:33:49+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** Operator ruling (2026-06-12, option c) on the Amendment 1 / auto-execution provenance question raised during Session A2 pre-GO review. (1) RATIFIED: the 2026-05-22 Phase 1 framework directive is AUTHENTIC — it was the operator's own typed chat message at 2026-05-22T15:54:21Z (session transcript 57451b12-030b-4860-84e8-8e4538f1f0b4, line 570, project C--Users-ahmed), followed by the operator's Q1-Q4 answers at 16:14:24Z (line 599, including verbatim 'Amendment 5 ... and Amendment 1 auto-execution layer are Phase 2 infrastructure builds'); backfilled entries 13-14 were stamped 16:02Z and commit 33d93ae landed 17:04Z — timeline locked. Recorded honestly: when the provision was surfaced on 2026-06-12 the operator did NOT recall it; authenticity rests on transcript evidence, not memory. (2) PROVENANCE REPAIR: the directive text was recovered VERBATIM from the transcript (body sha256 prefix 2d38aee392c82a48, 29,213 chars) and committed as ahmed_response_2026-05-22.md with a recovery header (source transcript id, recovery date, operator non-recall note) — the repo can now produce the file that commit 33d93ae, config.py, and strategy_register.md cite. (3) SUPERSESSION (forward-only): Amendment 1's activating condition ('auto-execution infrastructure exists') is PERMANENTLY VOID under the supervised operating model (entries 39-40); no such layer exists or is authorized. Amendment 1 is a permanently dormant DEAD LETTER; a SUPERSEDED banner was added to its strategy_register.md section pointing here, with the original text preserved unmodified as historical record. The substantive archetype-WR question (including its dSharpe>=1.5 / PF>=2.0 / CI>0 thresholds) MERGES into the entry-46 parked discussion item: merits-based, deliberate amendment process, decoupled from autonomy, forward-only. NO effect on any past verdict — the strict 0.40 floor bound every trial 1-40 including TSM-12 (the gate code only ever read min_win_rate; verified aig/validation_gate.py:120). (4) CONFIG CLEANUP: removed WR_FLOOR_BY_ARCHETYPE, WR_FLOOR_RELAXATION_THRESHOLDS, AUTO_EXECUTION_LAYER_EXISTS and their three PORTFOLIO_GATE wirings (latent-mislead risk; never read by gate code); corrected the config.py min_win_rate comment ('legacy strict floor; superseded by wr_floor_by_archetype' -> 'strict floor, binding for all archetypes — see entries 46/47'). Tests 14/14 green. config_hash changed 0c442a9c628f95af -> 2fa0e2f8cef4093d; GATE BEHAVIOR UNCHANGED (code reads only min_win_rate=0.40, before and after). (5) NEW OPERATIONAL RULE: any operator directive cited as a file MUST be saved and committed as that file in the same session it is received; chat-only directives are not citable as files.
- **rationale:** The pre-GO review found the repo citing a source document (ahmed_response_2026-05-22.md) it could not produce, for a provision (archetype WR-floor relief gated on auto-execution) the operator did not remember authorizing. A disk-wide provenance search resolved it: zero file hits anywhere, but the full directive exists as the operator's typed message in the session archive, with a locked timeline into commit 33d93ae. Ratifying on evidence while recording the non-recall keeps the record honest in both directions. Superseding rather than deleting preserves the append-only historical record while killing the latent risk: the directive's own framework-finality clause prescribes recording disagreement and acting only through a deliberate process, and entries 39-40 removed the autonomy premises (mandate, autonomous-agent architecture) that made an auto-execution layer conceivable — an activating condition that can never legally fire is a dead letter and saying so explicitly prevents a future session from treating it as live. The config knobs were the sharpest risk (config.py comment claimed the strict floor was 'superseded' — stronger than the directive itself); removing dead config that the gate never read changes the hash but not behavior, which this entry documents to keep provenance continuous. The archetype-WR question itself is legitimate and stays alive on its merits in the entry-46 parked item, now cleanly separated from the autonomy question it was originally coupled to.
- **alternatives_considered:**
  - Repudiate the amendment package entirely (rejected — the directive is authentically the operator's; repudiating documented authentic history on grounds of non-recall would corrupt the record)
  - Ratify as-is and leave Amendment 1 dormant-but-live (rejected — an activating condition voided by entries 39-40 cannot fire; leaving it nominally live invites a future session to build toward it or misread the register/config as a pending relaxation)
  - Delete the Amendment 1 section from the register (rejected — append-only discipline; banner + preserved text is the project's archival pattern, per entries 40/45 precedent)
  - Keep the config knobs with clarifying comments only (rejected — dead config wired into the PORTFOLIO_GATE dict is exactly where a future refactor could silently activate it; removal is the only state with zero latent risk, and the gate code's indifference makes it behavior-neutral)
- **audit_finding_refs:** `entry 46 (parked archetype-WR discussion item — merge target)`, `entries 39-40 (mandate retirement, v7 scope archival — autonomy premises removed)`, `entries 13-14 (backfilled Amendment 1/5 Phase 2 deferrals, 2026-05-22)`, `entry 32 (amendment-process precedent)`, `commit 33d93ae (framework directive commit, 2026-05-22T17:04Z)`, `transcript 57451b12-030b-4860-84e8-8e4538f1f0b4 lines 570/599 (operator's typed directive + Q&A)`, `aig/validation_gate.py:120 (gate reads only min_win_rate — verification that no past verdict was affected)`
### Pre-register TRB-50 (trial 41, trb50_us_1d) — second Phase 1 three-filter candidate, US slot 2, spec_hash a96ccdf5c0640e4f

- **ts_utc:** `2026-06-12T19:36:38+00:00`
- **methodology_source:** `three_filter_methodology`
- **decision:** TRB-50 (Trading Range Breakout per Brock/Lakonishok/LeBaron 1992: resistance = max(close) over prior 50 trading days shifted 1 bar; ENTRY when close > 1.01 x resistance; fixed 10-trading-day hold, exit at the 10th day's close; no stop, no leverage, long-only, re-entry only on a fresh breakout after flat) is pre-registered as trial 41 (trb50_us_1d, US-only) with spec_hash a96ccdf5c0640e4f, frozen at operator GO 2026-06-12 (Session A2, post entry-47 adjudication) BEFORE any test data was evaluated. Universe frozen at 1,603 tickers (universe/us_halal_full.txt, universe_sha256 6dfca4bd...775e4e bound inside the spec_hash; verified live this session — identical to the TSM-12-frozen value, zero dupes, no drift). n_trials_registered bumped 40 -> 41 in the same atomic commit as the trial-budget row, the canonical spec text, and the TRB50 config params (91fad62); tests 14/14 green at commit. PRE-DATA RULE-SET DECISIONS (operator-approved at GO, recorded per ruling): window=50 — BLL's primary variant and the highest-signal-frequency of the paper's {50,150,200}, chosen for statistical power toward the 1,000-trade portfolio floor; band=1% — a published variant of the paper's {0, 1%}, suppresses marginal penetrations; hold=10 trading days — the paper's own post-signal measurement window. None invented; all fixed before any data was touched. CANDIDATE DISPOSITIONS (operator approval covers both): C2 52-week-high (George & Hwang 2004, JF) -> DEFERRED QUEUE — the published form is cross-sectional ranking, which does not fit the per-ticker engine dispatch (F3), and a per-ticker adaptation would be building a variant before replicating the original (source-priority rule 2); C3 Donchian 20/10 (Turtle) -> REJECTED — T2 evidence and a near-duplicate of Pre-Framework DBO, which already failed honestly on the WR floor (re-selecting it would be iterating on a failed strategy). Engine implementation of the frozen spec follows pre-registration and precedes the staged_validate run, with A1 guardrails: spec-exact code, zero parameters beyond spec, edge-case conventions inherited from trials 1-40 and documented in the verdict entry (entry 49). Validation isolation via cron_paused.flag per the A1 pattern.
- **rationale:** Three filters applied live against the register: F1 archetype diversity — Phase 1 cleared is Divergence (pullback) only; trend_following (priority 1) was tested and failed honestly (TSM-12, trial 40, entry 46), so breakout (priority 2) is next; the only prior breakout (Pre-Framework DBO) failed honestly and is not re-selectable. F2 evidence tier — T1: BLL 1992 is peer-reviewed (Journal of Finance), ~90 years of DJIA data, independently replicated across markets (Bessembinder & Chan 1995; Hudson/Dempsey/Keasey 1996); recorded pre-data for honesty: Sullivan/Timmermann/White 1999 found the BLL rules' profits fade after data-snooping adjustment post-1986 — the gate adjudicates, failure is acceptable. F3 readiness — daily bars suffice, long-only (Rule 15), no leverage (Rule 16), halal universe, per-ticker time-series form fits the engine dispatch; the no-stop convention exists from TSM-12. Structural note recorded pre-data: TRB-50 faces the strict 0.40 WR floor (entry 47 made its permanence explicit); six trend/momentum-family near-misses failed only that floor, and the fixed 10-day hold's WR is an open empirical question — this is a hypothesis test, not a promise.
- **alternatives_considered:**
  - C2: 52-week-high proximity (George & Hwang 2004) — deferred queue, not test queue (cross-sectional form incompatible with per-ticker engine; adaptation = variant before replication)
  - C3: Donchian 20/10 Turtle-style — rejected (T2; near-duplicate of failed Pre-Framework DBO; iterating on failed strategies is forbidden)
  - Window 150 or 200 from the paper's rule set — rejected pre-data (fewer signals; the 1,000-trade floor at monthly-scale signal frequency already killed UAE trials; statistical power favors 50)
  - Band 0% variant — rejected pre-data (marginal penetrations add noise trades; 1% is equally published)
  - Open-ended trend exit instead of fixed hold — rejected (not in the paper's TRB measurement; would be an invented parameter)
- **audit_finding_refs:** `entry 46 (TSM-12 verdict — trend_following tested-and-failed, slot 2 open)`, `entry 47 (Amendment 1 adjudication — strict 0.40 floor permanence; provenance rule)`, `entry 45 (TSM-12 pre-registration — spec_hash derivation convention, Path 2d-A pattern)`, `entry 8 (Phase 1 cap and slot structure)`, `entry 32 (framework amendment)`, `commit 91fad62 (atomic pre-registration)`
### TRB-50 verdict (trial 41, trb50_us_1d): PORTFOLIO_CLEARED_FOR_PAPER_FORWARD — US slot 2 FILLED

- **ts_utc:** `2026-06-12T20:39:33+00:00`
- **methodology_source:** `three_filter_methodology`
- **decision:** TRB-50 (trial 41, trb50_us_1d, spec_hash a96ccdf5c0640e4f) CLEARED the validation gate at portfolio level: verdict PORTFOLIO_CLEARED_FOR_PAPER_FORWARD (passed=true, reasons empty) at 2026-06-12T20:37:18Z (validation_trb50_us_1d.json, staged run, 9 batches, live data, config_hash c7ff799942e2c8da = the pre-registration commit's hash). Full numbers: 25,760 portfolio trades from 1,115 contributing tickers of 1,122 evaluated (universe coverage 99.38% — highest of any trial to date); portfolio expectancy 1.1521; win rate 0.5097 (clears the strict 0.40 floor by 11 points — the fixed 10-day-hold hypothesis from entry 48 CONFIRMED: first breakout-archetype strategy to clear the floor after six trend/momentum-family WR-floor near-misses); Sharpe raw 3.4834, deflated 2.8021 under the n=41 registered-trials haircut (5.6x the 0.5 floor); 95% CI on mean trade [+0.003176, +0.005205] — strictly positive. All gate criteria passed; per-ticker gate additionally cleared 1 ticker (the portfolio claim is the binding one). US SLOT 2 IS FILLED: Phase 1 slots now 1=Divergence (pullback), 2=TRB-50 (breakout), 3-4 open. UNIVERSE CONVENTION (entry 46 pattern): spec froze 1,603 tickers (universe_sha256 6dfca4bd...775e4e, verified live pre-run — no drift); engine evaluated 1,122. The 481 exclusions follow the trials 1-40 pre-backtest data-integrity convention: (a) DATA_ERROR, 63 tickers — yfinance fetch raised or returned an empty frame (delisted/renamed); no synthetic fallback, ticker skipped; (b) BLOCKED_DATA, 418 tickers — integrity_check in aig/data.py: 394 unadjusted split/spike suspected (single-bar |return| > 50%), 23 insufficient history (< 300 daily rows), 2 high/low/close inconsistency. Identical exclusion profile to trial 40 (entry 46) — same gate, same universe, consistent. Excluded tickers do not enter portfolio aggregation; the per-ticker deflation haircut used the full frozen n=1,603, the portfolio haircut n=41. ENGINE CONVENTIONS ADOPTED (documented per entry 48 guardrail, all in aig/strategy_trb50.py header): position-aware signal pass exactly mirroring the engine state machine (first such strategy — fixed-N-day holds are inexpressible as stateless per-row exits); entry is the frozen LEVEL condition close > 1.01 x resistance evaluated while flat (the trailing close-max makes it a de-facto penetration event); exit_signal exactly 10 bars after each actual entry bar; same-bar exit-then-reentry impossible (engine elif), earliest re-entry the following bar; 50-bar warmup via the ema:=resistance NaN sentinel; no stop via infinite _stop_distance (TSM-12 convention); open position at end of history discarded. 6 conformance tests, suite 20/20 green at implementation commit 715e38d. RUN INTEGRITY: the first launch attempt (2026-06-12 ~19:57Z) died 5 minutes into batch 0 on host disk-full (C: 0 bytes free) with ZERO partial state written and ZERO results produced (queue verified at next_index 0); after reclaiming ~3.9GB of regenerable caches the run executed start-to-finish in ONE clean pass (31 minutes). This was an infrastructure recovery, not a re-run — no data had been evaluated into any result before the restart. NEXT STEP (operator-supervised, NOT done this session): paper-forward deployment of TRB-50 per the frozen watch-list protocol (selection from the 1,115-ticker cleared-contributor set pre-registered BEFORE deployment, per Improvement 1 / NEW-1 precedent); until then TRB-50 is cleared-not-deployed. Track 1 resumes (flag removal follows this entry).
- **rationale:** The result is reported with the same discipline as the entry-46 FAIL: spec frozen before data (entry 48, commit 91fad62), engine spec-exact (715e38d, 20/20 tests), one clean run, verdict recorded verbatim from the same gate code that failed trials 1-40. Why this cleared where the trend family failed: the fixed 10-trading-day hold exits before breakout gains mean-revert away, producing a ~51% win rate versus the ~23-35% of open-ended trend exits, while the breakout entry preserves the positive expectancy — exactly the mechanism the pre-data rationale in entry 48 hypothesized, now confirmed rather than assumed. Honest context recorded pre-data and still true: Sullivan/Timmermann/White 1999 showed BLL-style rule profits fade post-1986 after data-snooping adjustment; this run's evidence is from a 41-trial-haircut deflated Sharpe of 2.80 on 25,760 trades on data through 2026, which the gate accepts as clearing the pre-registered bar — the deflation haircut exists precisely to price the data-snooping risk the STW critique names. Deployment is deliberately NOT bundled into this entry: the watch-list protocol requires pre-registered selection from the cleared universe before any signal fires, and rushing it inside the verdict session would repeat the cherry-picking failure mode audit NEW-1 flagged.
- **alternatives_considered:**
  - Bundle paper-forward deployment into this session (rejected — watch-list selection must be pre-registered per the frozen protocol; done deliberately, operator-supervised, next session)
  - Treat the disk-full restart as a second trial / haircut event (rejected — zero results existed before the restart; the trial evaluated data exactly once; documented transparently instead)
  - Discount the verdict for the STW 1999 critique post-hoc (rejected — the gate's deflation haircut is the pre-registered mechanism for that risk; moving the goalposts after a PASS is the same sin as moving them after a FAIL)
- **audit_finding_refs:** `entry 48 (TRB-50 pre-registration, pre-data rule-set decisions)`, `entry 46 (TSM-12 verdict — exclusion-documentation pattern, slot 2 history)`, `entry 47 (strict 0.40 floor permanence — the floor TRB-50 cleared on merits)`, `entry 8 (Phase 1 cap and slot structure)`, `Session 5 audit NEW-1 + Improvement 1 (watch-list protocol governing the deployment next step)`, `commits 91fad62 (pre-registration), 715e38d (implementation), c95fe0d (entry 47 adjudication)`, `validation_trb50_us_1d.json (provenance utc 2026-06-12T20:37:18+00:00, config_hash c7ff799942e2c8da)`
### Deploy TRB-50 to paper-forward (slot 2): watch-list pre-registration + second detector + routing reconciliation

- **ts_utc:** `2026-06-13T08:50:32+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** TRB-50 (trial 41, cleared entry 49) is deployed to paper-forward as Phase 1 slot 2, under the operator-approved plan (2026-06-12 22:12Z) with binding Amendments A/B/C. Executed under cron_paused.flag isolation, committed atomically. (1) WATCH-LIST PRE-REGISTRATION (register section + frozen file, the NEW-1 template): source population = the 1,115 contributing tickers (oos_n > 0) from validation_trb50_us_1d.json (config_hash c7ff799942e2c8da, spec_hash a96ccdf5c0640e4f); selection = Improvement 1 Option A, the ENTIRE cleared-contributor set, no subsetting (operator ruling; precedent 33d93ae); frozen artifact universe/trb50_us_paperforward_watchlist.txt (1,115 symbols) committed; audit tag full_cleared_contributor_set_n1115_from_validation_trb50_us_1d_cfg_c7ff799942e2c8da_entry50_2026-06-13 written into state on every run. The 7 evaluated-but-zero-signal tickers (STRK, STRF, TEM, LOAR, WAY, TTAM, WFF) are excluded on a deterministic NON-performance basis — they produced no OOS signals under the frozen spec. Improvement 2 (sector/liquidity constraints) recorded N/A under Option A (no sampling step to constrain; operator-confirmed). (2) DETECTOR (scripts/paper_forward_trb50.py): AMENDMENT A satisfied by IMPORT — the entry signal is computed by `from aig.strategy_trb50 import signals`, the exact frozen-spec module the engine dispatches to; a conformance test asserts `pf.signals is engine.signals` (identity, not copy). The one forward-specific mechanic is the fixed 10-trading-day hold, driven by the PAPER position's own entry_date via a bar-count (bars >= TRB50_HOLD_DAYS strictly after entry), idempotent and no-new-bar-safe; no stop (frozen spec); same-bar exit-then-reenter blocked mirroring the engine elif. 6 conformance tests green (bar-count exit, exact-N boundary, no-new-bar no-op, same-bar guard, no-stop, Amendment-A identity); engine suite 20/20 unaffected. AMENDMENT C: TRB-50 Telegram is DIGEST-ONLY (one summary block per fire), the Divergence per-signal contract untouched; migrating Divergence to digest later is a logged operational change. (3) AMENDMENT B ROUTING RECONCILIATION: winners_assignment.json rebuilt with cleared sources = {divergence (slot 1), trb50 (slot 2)} — 1,122 tickers assigned (510 divergence, 612 trb50, 1,023 in both reassigned by best dSharpe). Pre-Framework mbv/pmr/str were tagged out (PRE_FRAMEWORK_TAGGED_OUT) per the entry-32 grandfathering (only divergence carried into Phase 1); their 5 open paper positions in paper_forward_positions_full.json (CARR, CMI, ADI, MUR, GGDVY — all mbv, entries 2026-05-21) were administratively closed (state key administratively_closed_preframework, with timestamp + reason; history preserved, NOT Phase-1 evidence) rather than orphaned silently. The full-universe detector (paper_forward_full_universe.py) is marked DORMANT in its header; the LIVE detectors driven by fires are the two per-strategy scripts. (4) SKILL step 2 updated to run BOTH detectors foreground in order, with a sentinel mark between them; end-of-fire summary template adds the slot-2 line; sprint_catchup brief now reads both per-strategy state files. NO gate/config changes (config_hash untouched at c7ff799942e2c8da); the only constants the detector uses are the frozen TRB50_* params. Track 1 resumes on flag removal; the first two-detector fire is then verified end-to-end. RUN/HOST NOTE: disk precondition cleared by operator (Recycle Bin emptied ~12.7GB; ~15GB free at build); separately, the host showed memory pressure during the build (git 'paging file too small' once, recovered) consistent with the accumulated 2026-06-11 orphan claude.exe workers — flagged for operator zombie_kill, not killed unattended.
- **rationale:** Deployment is the natural consequence of the entry-49 clear, but it carries the audit-NEW-1 cherry-pick risk, so the watch-list selection is pre-registered (full cleared universe, no expectancy filter) before any signal fires — the same discipline that protected the Divergence deployment. Amendment A (import, not re-implement) makes spec drift between the validated engine and the deployed detector structurally impossible: there is exactly one signals() definition, and a test enforces the identity. The fixed-hold exit is the one place the forward path legitimately differs from a stateless detector, so it is driven by the durable paper-book entry date (not the re-simulated history, which a yfinance backfill could shift) and unit-tested for idempotency. Amendment B closes a real latent inconsistency the 2026-05-26 maintenance entry flagged: winners_assignment.json was still routing Pre-Framework mbv/pmr/str that the entry-32 amendment grandfathered out — adding trb50 was the honest moment to reconcile the deployed surface to the framework, and the open positions were closed administratively (not deleted) so their history survives without being miscounted as Phase-1 evidence. Amendment C (digest for the higher-frequency strategy) controls the ~30+ msg/day alert volume TRB-50 would add without touching the frozen spec, since alert format is operational. Nothing here changes the gate, the config hash, or any verdict.
- **alternatives_considered:**
  - Re-implement the breakout rule inside the detector for speed (rejected — Amendment A; import is the single source of truth, drift-proof)
  - Drive the fixed-hold exit off the re-simulated module exit flag (rejected — a history backfill could move the simulated entry relative to the recorded paper entry; the durable paper entry_date is the correct clock)
  - Leave mbv/pmr/str routing in place and only add trb50 (rejected — perpetuates the entry-32 inconsistency the 2026-05-26 entry flagged; Amendment B rules reconcile)
  - Delete the 5 pre-framework open positions (rejected — administrative close preserves history; deletion would erase the record)
  - Per-signal Telegram for TRB-50 like Divergence (rejected — Amendment C; ~30+ msgs/day; digest keeps signal-to-noise sane without touching the spec)
  - Extend paper_forward_full_universe.py for trb50 instead of a new script (rejected — its generic exit logic has no fixed-hold bar-count state; a clean per-strategy detector matching the divergence pattern is lower-risk and testable)
- **audit_finding_refs:** `entry 49 (TRB-50 verdict CLEARED — the deployment basis)`, `entry 48 (TRB-50 pre-registration)`, `entry 32 (amendment grandfathering — only divergence into Phase 1; basis for Amendment B)`, `Session 5 audit NEW-1 + Improvement 1 (watch-list protocol — Option A full cleared universe)`, `2026-05-26 maintenance entry (flagged the mbv/pmr/str routing inconsistency Amendment B resolves)`, `operator approval 2026-06-12 22:12Z (Amendments A/B/C, sequence, precondition)`, `files: scripts/paper_forward_trb50.py, universe/trb50_us_paperforward_watchlist.txt, scripts/reassign_best_strategy.py, winners_assignment.json, tests/test_paper_forward_trb50.py`
### Relocate Track-1 driver off OneDrive to C:\aig_engine

- **ts_utc:** `2026-06-15T08:47:07+00:00`
- **methodology_source:** `infrastructure_decision`
- **decision:** Track-1's headless sprint driver (the repo + its two scheduled tasks) was relocated from the OneDrive-synced path (C:\Users\ahmed\OneDrive\Documents\Projects\stocks\Ahmed group\Working Area\aig_engine) to a plain local path C:\aig_engine. ROOT CAUSE (experimentally isolated this session; supersedes the IPv6 mis-diagnosis 71d08fb): every headless `claude -p` worker runs the global ~/.claude/CLAUDE.md MANDATORY session-start recovery scan (git status + read HANDOFF + list flags) BEFORE the SKILL; those tool-calls executed against the cloud-only (RECALL_ON_DATA_ACCESS) OneDrive project dir, and under OneDrive thrash they stalled on hydration with no client timeout -> indefinite first-turn hang (no transcript ever written; the model's API socket sat ESTABLISHED-but-idle, which is why it looked like an API/IPv6 hang). RULED OUT by experiment: IPv6 (the post-ipv4first-fix worker still hung, on IPv4), auth (manual -p completes full turns in good windows), MCP (hangs persist with --strict-mcp-config empty), stdin (EOF-stdin runs both work and hang), the SKILL/prompt (a trivial 'Reply OK' hangs the same), the CLI binary (unchanged 06-13), raw network (HEAD api.anthropic.com 0.2s, no proxy). DECISIVE A/B: trivial -p from the OneDrive cwd hung 1/3 and ran 37-70s; from %TEMP% 0/3 hung. MOVE (operator-approved, gated on 0 live workers, atomic): disabled both tasks -> committed+pushed the clean tree (48f938e) -> robocopy /MOVE (preserves .git + tracked + untracked + gitignored-local) -> verified git intact (HEAD 48f938e == origin, remote UNCHANGED) -> updated 6 refs ($ProjectRoot in sprint_task_launcher.ps1, cc_watchdog.ps1, cc_watchdog_test.ps1, install_watchdog.ps1 + the AIG-Mode1-Sprint and AIG-CC-Watchdog task actions: -File path AND WorkingDirectory) committed (fab7d6c). VERIFIED by one SUPERVISED MANUAL fire from C:\aig_engine BEFORE re-enabling tasks: transcript written in ~6s in the new transcript dir (C--aig-engine) - the exact thing that NEVER happened during the OneDrive hangs - both detectors ran, sprint commit 6609717 landed + pushed, sentinel advanced, worker exit=0. THEN both tasks re-enabled and OneDrive resumed. Paired with Fix 3 (94e28ca: launcher verify-kill reap loop so a hung worker can't outlive the cap and jam the IgnoreNew slot, + BASH_DEFAULT_TIMEOUT_MS). Git remote unchanged (https://github.com/ahmedvipha-afk/aig-engine-state.git).
- **rationale:** OneDrive on the live repo was the trigger; pinning/hydrating (option 2a) proved insufficient and OneDrive was actively self-thrashing (CPU 7,485s -> 21,955s over the night). A plain local path removes the OneDrive sync engine from EVERY git/file op - the durable cure - while Fix 3 removes the amplifier (orphaned hung workers jamming the IgnoreNew slot for hours). The new path was proven by a supervised fire BEFORE any scheduled fire could touch it, so a broken path could never auto-fire. The move preserved the actual .git + all untracked/gitignored-local files (robocopy /MOVE, not a fresh clone).
- **alternatives_considered:**
  - Pin repo 'Always keep on this device' (2a) - rejected: partial, OneDrive still mediates and was actively thrashing
  - Disable the per-spawn recovery scan via launcher prompt (fix 1) - rejected: may lose to the MANDATORY CLAUDE.md scan and leaves the SKILL's own OneDrive I/O exposed
  - git clone fresh instead of move - rejected: drops untracked + gitignored-local files; robocopy /MOVE preserves everything
  - Bump launcher/task timeouts - rejected explicitly: bumping timeouts to outrun a hang was the wrong instinct
- **audit_finding_refs:** `2026-06-14 root-cause diagnosis (this session): experimental elimination of IPv6/auth/MCP/stdin/network + OneDrive-cwd vs TEMP-cwd A/B`, `Fix 3 commit 94e28ca (verify-kill reap loop)`, `pre-move baseline 48f938e; ref-update fab7d6c; supervised-fire sprint 6609717`, `supersedes 71d08fb (ipv4first / IPv6 mis-diagnosis; left in place as harmless)`, `git remote unchanged: github.com/ahmedvipha-afk/aig-engine-state.git`, `files: scripts/{sprint_task_launcher,cc_watchdog,cc_watchdog_test,install_watchdog}.ps1; scheduled tasks AIG-Mode1-Sprint, AIG-CC-Watchdog`
### Integrity gap: 2026-05-22 amendments unenforced by gate code

- **ts_utc:** `2026-06-15T20:41:07+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** FINDING (logged on discovery, before any fix, per audit discipline): the six 2026-05-22 Phase-1 framework amendments are DEFINED in config.py PORTFOLIO_GATE but aig/validation_gate.py portfolio_evaluate() reads NONE of the amendment keys. config.py defines them: min_trades_by_market (L426, Amendment 2; constant MIN_TRADES_BY_MARKET L339-344), min_profit_factor=1.5 (L427) / min_profit_factor_ci_lower=1.0 (L428) / profit_factor_min_n=30 (L429) (Amendment 3; constants L350-352), min_oos_calendar_months=24 (L430, Amendment 6; constant L374), min_oos_to_is_sharpe_ratio=0.7 (L431, Amendment 2-extension; constant L380), gcc_universe_enabled=False (L432, Amendment 5, Phase-2-deferred anyway). portfolio_evaluate() (validation_gate.py L71-165) reads ONLY the legacy six: g['min_trades'] (L111), g['min_expectancy'] (L115), g['min_win_rate']=0.40 (L120), g['min_universe_coverage'] (L124), g['min_oos_sharpe']=0.5 (L132), and the bootstrap CI>0 check (L137-140). It never references min_profit_factor, min_profit_factor_ci_lower, profit_factor_min_n, min_oos_calendar_months, min_oos_to_is_sharpe_ratio, or min_trades_by_market. config.py L407 itself labels the enforced subset 'Legacy keys (preserved for back-compat with existing validation_gate.py)' -- i.e. the amendment block (L425-432) was added to config WITHOUT updating the evaluator to read it. IMPACT: every portfolio verdict to date was produced by the ORIGINAL gate, not the amended one -- both the clears (Divergence trial 4, MBV trial 7, PMR trial 24, STR trial 27, TRB-50 trial 41 = Phase-1 slots 1-2 plus the paper-forward deployments) AND the fails (dbo trial 10, roc 13, vcb 16, art 30, cmf 33, gap 36 = the WR-floor-only near-misses; tsm12 trial 40). The amended gate the register presents as binding (PF>=1.5 + PF-CI>=1.0, OOS-Sharpe>=0.7xIS, OOS span>=24 months, per-market trade-count floors) has NEVER executed against any trial. SAME CLASS as entry-47's Amendment-1 finding (config knobs the gate never read; admitted at config.py L327) but BROADER: 5 of the 6 core amendments are unenforced (Amendment 4 is a process rule with no config knob; Amendment 5/GCC is Phase-2-deferred and irrelevant to US/UAE/Crypto). NO VERDICT IS CHANGED BY THIS ENTRY -- it RECORDS the gap. Remediation (wire the amendment keys into portfolio_evaluate, then re-validate the affected trials) is a SEPARATE scoped decision, NOT done here; its scope is laid out for council/operator review alongside this entry. Discovered 2026-06-16 during the entry-46 WR-floor evidence-assembly step, which is downstream of this finding and is PAUSED pending its resolution.
- **rationale:** Audit discipline: an integrity gap is logged the moment it is found, before any fix, so the record honestly fixes WHEN the gap existed (2026-05-22 amendment commit -> 2026-06-16 discovery) and is not entangled with the remediation. The gap is load-bearing for the entry-46 WR-floor brief: that brief's evidence base ('six clean WR-floor-only fails', 'the clears') rests entirely on ORIGINAL-gate verdicts -- the council must know the other five amendment criteria were never enforced before it debates whether to vary the WR floor. Logging now also prevents the gap from being silently closed by a future gate edit with no provenance trail.
- **alternatives_considered:**
  - Fix the gate first, then log (rejected -- log-on-discovery is the audit discipline; fixing first entangles the record with the remediation and erases the honest 'gap existed from 2026-05-22 to 2026-06-16' fact)
  - Treat the gap as verdict-invalidating immediately and pull the cleared slots (rejected -- recording is not remediation; verdicts stand as produced until a scoped re-validation decision; pulling now would be an unreviewed over-reaction)
  - Fold this into the entry-46 WR-floor entry (rejected -- this finding outranks and is independent of the WR-floor question; it earns its own entry and its own remediation track)
- **audit_finding_refs:** `config.py L425-432 (amendment keys defined in PORTFOLIO_GATE) + L339-352/L374/L380 (constants) + L407 ('back-compat with existing validation_gate.py') + L327 (entry-47 same-pattern admission)`, `aig/validation_gate.py portfolio_evaluate() L71-165 reads only L111/L115/L120/L124/L132/L137-140 (the legacy six criteria)`, `entry 47 (Amendment-1 knobs the gate never read -- same class, narrower)`, `entry 46 (WR-floor parked question -- downstream of this finding; paused)`, `affected verdicts (all original-gate): trials 4/7/24/27/41 CLEARED; 10/13/16/30/33/36 WR-floor-only FAIL; 40 tsm12 FAIL`
### expectancy == profit factor; documented PF>=1.5 uncleanable

- **ts_utc:** `2026-06-15T20:51:02+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** FINDING (builds on entry 52; logged for the council, no fix applied): the codebase's `expectancy` metric IS the profit factor. aig/stats.py expectancy() (L16-24) computes (wins.mean()*wr)/(abs(losses.mean())*lr); since wins.mean()*wr = sum(wins)/n_total and abs(losses.mean())*lr = sum(|losses|)/n_total, it reduces exactly to sum(wins)/sum(|losses|) = profit factor. So the gate's ENFORCED min_expectancy=1.0 (config.py L410) already imposes PF>=1.0, and Amendment 3's min_profit_factor=1.5 (config.py L427; constant MIN_PROFIT_FACTOR L350) is the SAME statistic at a higher bar -- not an independent criterion. IMPACT: no trial in the budget has ever reached PF 1.5. Profit factors (= the reported `expectancy`) by trial -- CLEARED: divergence 1.227 (trial 4), mbv 1.302 (7), pmr 1.268 (24), str 1.103 (27), trb50 1.152 (41); WR-FLOOR-ONLY FAILS: dbo 1.298 (10), roc 1.217 (13), vcb 1.187 (16), art 1.209 (30), cmf 1.396 (33), gap 1.181 (36); tsm12 1.0537 (40). All-time maximum = cmf 1.396 < 1.5. Therefore enforcing the documented amended gate AS WRITTEN fails EVERY strategy ever tested -- including live Phase-1 slot 1 (Divergence), slot 2 (TRB-50), and all paper-forward deployments -- on Amendment 3 alone; the documented gate is, as written, UNCLEANABLE by anything in the observed PF range. IMPLICATION: this is NOT a wiring bug to fix mechanically. The documented threshold (PF>=1.5) appears to have been set WITHOUT a feasibility check against achievable profit factors (1.5 exceeds the observed maximum 1.396). Wiring it literally would un-clear the entire portfolio. This is the SAME CLASS of error as a target set without a feasibility check -- an aspirational number never reconciled with reality, one level down from the retired 3x/10x phantom-mandate pattern (entry 39). NO VERDICT IS CHANGED BY THIS ENTRY -- it records the finding for the council/cross-check round, which owns any threshold/definition reconciliation. Discovered 2026-06-16 while scoping the entry-52 remediation.
- **rationale:** The council cannot adjudicate the gate-integrity question (entry 52) without knowing that the headline amendment criterion (PF>=1.5) is not merely unenforced but, on the observed data, UNACHIEVABLE -- and that it duplicates an already-enforced metric (expectancy) at an infeasible threshold. Logging the identity and the per-trial PF values now gives the council the quantitative basis and prevents a mechanical 'just wire in the config keys' remediation that would silently invalidate the working portfolio.
- **alternatives_considered:**
  - Treat PF>=1.5 as a sound bar and re-validate against it (rejected here -- recording is not deciding; whether 1.5 is the intended/sound bar is the council's call, not a unilateral one)
  - Assume 'PF' was meant as a different statistic than expectancy and quietly substitute it (rejected -- the code is unambiguous: expectancy() == PF; any reinterpretation is a council/spec decision, not an inference)
  - Defer logging until the council convenes (rejected -- audit discipline logs the quantitative finding on discovery, same as entry 52)
- **audit_finding_refs:** `aig/stats.py L16-24 (expectancy() == sum(wins)/sum(|losses|) == profit factor)`, `config.py L410 (min_expectancy=1.0, ENFORCED) vs L427/L350 (min_profit_factor=1.5, UNENFORCED -- same metric, higher bar)`, `per-trial PF(=expectancy) from strategy_register.md TRIAL_BUDGET (trials 4/7/10/13/16/24/27/30/33/36/40/41); all-time max cmf 1.396 < 1.5`, `builds on entry 52 (amendments defined-but-unenforced)`, `entry 39 (retired 3x/10x phantom-mandate -- the parent pattern: aspirational number not feasibility-checked)`
### Council round (degraded): single-external-opinion input

- **ts_utc:** `2026-06-15T21:14:46+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** RECORD of the council/cross-check round convened on the validation-gate-integrity brief (council_brief_gate_integrity_2026-06-16.md, commit 9ad054b; builds on entries 52+53). The round ran DEGRADED: the Tier-2 true-council (external models via OpenRouter) had all three primary seats fail/rate-limit -- Kimi K2 (moonshotai/kimi-k2:free) and DeepSeek R1 (deepseek/deepseek-r1:free) BOTH fell back to the SAME substitute model (openai/gpt-oss-120b:free), and Gemini 2.5 Pro (google/gemini-2.5-pro-exp + substitutes) 404'd entirely. So the round was effectively ONE external model heard twice + Claude as Chairman -- NOT the 3-model diversity intended. DIRECTIONAL ANSWER (recorded as a SINGLE-EXTERNAL-OPINION input, explicitly NOT a 3-0 consensus): keep the evidenced CODE gate as operative; remediate by FIXING THE DOCS (remove or re-label the unattainable PF>=1.5, which duplicates the already-enforced expectancy>=1.0 i.e. PF>=1.0); do NOT lower any threshold to admit the existing portfolio. The model added one substantive instrument beyond the brief: a formal 'Gate Specification Update' recording the operational gate AND requiring a feasibility simulation before any FUTURE gate revision deploys (institutionalizing the missing feasibility check). LIMITATIONS (explicit, so this is weighted correctly later): (i) one model not three -- convergence is not corroboration; (ii) the prompt carried the hard constraint + evidentiary asymmetry, so the answer is partly downstream of FRAMING, not independent; (iii) questions (c) [is 'PF' a different statistic than expectancy?] and (e) [forward-only vs re-validate specifics] were under-addressed -- the seats ASSUMED PF==expectancy. NO DECISION MADE; no code/config/gate changed. This entry records the round as INPUT to the operator's pending decision, which awaits the question-(c) provenance dig and a genuine multi-model re-run when seats verify healthy.
- **rationale:** Audit honesty: a cross-check round that degraded must be recorded AS degraded so it is weighted correctly (one external opinion, framing-influenced) and not later mistaken for a 3-model consensus. Recording the directional answer plus its limitations preserves the input without inflating its evidentiary weight -- the same discipline that logged the enforcement gap (entry 52) and the expectancy==PF math error (entry 53).
- **alternatives_considered:**
  - Record it as a council consensus (rejected -- it was one model heard twice; calling it consensus is the exact inflation the audit discipline guards against)
  - Do not record a degraded round at all (rejected -- the round happened and informed thinking; an unrecorded cross-check is a provenance hole)
  - Re-run immediately to force diversity (rejected for THIS entry -- free seats are rate-limited; re-run only when seats verify healthy, as a separate step)
- **audit_finding_refs:** `council_brief_gate_integrity_2026-06-16.md (commit 9ad054b) -- the deliberated brief`, `builds on entries 52 (amendments defined-but-unenforced) + 53 (expectancy==PF, PF>=1.5 uncleanable)`, `seats actually used: kimi-k2:free + deepseek-r1:free BOTH -> openai/gpt-oss-120b:free; gemini-2.5-pro + substitutes 404`, `OPEN: question (c) provenance dig + a genuine 3-model re-run on the same brief, pending healthy seats`
### Amendment-3 PF>=1.5 intentional; remediation is operator-intent

- **ts_utc:** `2026-06-15T21:24:03+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** PROVENANCE RESULT (builds on entries 52/53/54; resolves council question (c)). The 2026-05-22 source directive (ahmed_response_2026-05-22.md, Amendment 3) states: 'Every cleared strategy must have OOS Profit Factor >= 1.5', with an n>=30 guard ('one outsized winner can produce PF > 3 by accident') and a bootstrap-95%-CI-on-PF lower bound >= 1.0 ('break-even on a profit-factor basis'). Those glosses confirm 'Profit Factor' = the STANDARD definition = gross profit / gross loss = sum(wins)/sum(|losses|) = the code's expectancy() EXACTLY (aig/stats.py L16-24). So question (c) resolves: 'PF' is NOT a different statistic (it equals expectancy), BUT PF>=1.5 was a DELIBERATE TIGHTENING above the enforced expectancy>=1.0 floor -- an intentional operator-authored hard requirement sitting among the other Amendment tightenings -- NOT an accidental or redundant duplicate. CONSEQUENCE: the degraded council's directional answer was HALF-RIGHT (PF==expectancy; the amended gate was never wired) and HALF-WRONG (it treated PF>=1.5 as deletable redundancy and recommended 'fix docs to PF>=1.0'; provenance shows that would RELAX the operator's OWN intended bar to admit the existing portfolio = the forbidden goalpost-gravity direction the brief's hard constraint names). The remediation is therefore an OPERATOR-INTENT question, not a mechanical code/docs fix: (A) PF>=1.5 was a deliberate high bar the current portfolio legitimately fails -> wire it AS WRITTEN, ACCEPT that it un-clears the existing portfolio (including live slots 1-2), and keep searching; OR (B) PF>=1.5 was set without feasibility-checking against achievable PFs (observed all-time max = cmf 1.396) -> recalibrate to a feasibility-checked value as a DELIBERATE operator-authored standard, explicitly NOT a relaxation reverse-engineered to admit current candidates. Only the operator can rule A vs B -- it turns on original intent, which neither code analysis nor an external council can supply. NO DECISION MADE; no code/config/gate changed. The council is HELD: seats are dead (all three :free model IDs 404) AND the live question is operator-intent, which an external council cannot answer; a future council is reserved ONLY for the narrow model-debatable sub-question (if the operator rules B, which recalibration method is sound).
- **rationale:** The provenance dig is load-bearing because it inverts the cheap reading. 'PF>=1.5 is a meaningless duplicate, delete it' is WRONG -- the operator deliberately authored it as a tightening, confirmed by the directive's own hard wording and its PF-specific guards (n>=30, bootstrap-CI). That means the choice between enforcing it (un-clearing the portfolio) and recalibrating it cannot be made by code analysis or by an external council; it depends on what the operator INTENDED, and on whether any recalibration is a feasibility-correction (legitimate) or a portfolio-rescue (forbidden). Recording this prevents the audit trail from later treating the degraded council's 'fix docs to 1.0' as a settled answer.
- **alternatives_considered:**
  - Accept the council's 'fix docs to PF>=1.0' (rejected -- provenance shows 1.5 was intentional; lowering to 1.0 to admit the portfolio is the goalpost-gravity the hard constraint forbids unless 1.5 is affirmatively ruled an error by the operator)
  - Decide A vs B from the data myself (rejected -- A vs B turns on operator INTENT, not data; deciding it would usurp the operator and risk the forbidden direction)
  - Hunt fresh council seats and re-run now (rejected -- the live question is operator-intent, which a council cannot answer; reserve a council only for B's recalibration-method sub-question)
- **audit_finding_refs:** `ahmed_response_2026-05-22.md Amendment 3 (PF>=1.5 'must', n>=30 guard, bootstrap-CI-on-PF >= 1.0) -- intentional tightening; gloss confirms standard PF = gross/gross`, `aig/stats.py L16-24 (expectancy() == sum(wins)/sum(|losses|) == standard profit factor)`, `builds on entries 52 (gate never wired) + 53 (expectancy==PF, PF>=1.5 uncleanable, max 1.396) + 54 (degraded council, one-external-opinion)`, `council answer status: HALF-RIGHT (PF==expectancy, gate unwired) / HALF-WRONG (treated 1.5 as deletable redundancy)`, `OPEN: operator A-vs-B intent ruling; council reserved only for the B recalibration-method sub-question`
### Correction to 53/55: PF>=1.5 IS achievable (ema200 1.556)

- **ts_utc:** `2026-06-15T22:08:32+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** CORRECTION to entries 53 and 55 (append-only; those entries stand as historical record, this entry supersedes their specific erroneous claim). Entries 53/55 asserted 'all-time max profit factor 1.396 (cmf); no trial ever reached PF 1.5; the documented gate is uncleanable as written.' THAT IS FACTUALLY WRONG. Verified from the validation artifacts (portfolio_expectancy field = PF, since expectancy()==PF per entry 53): ema200_us (validation_us_ema200_1d_full.json, provenance run 2026-05-20T17:06:38Z) has portfolio PF = 1.5556 (> 1.5). It FAILED the gate on WIN RATE (WR 0.1863 << 0.40), not on PF. So PF>=1.5 is ACHIEVABLE; the correct statement is that no strategy has reached PF>=1.5 WHILE ALSO passing WR>=0.40 -- the high-PF strategies are low-WR and vice versa (a PF-vs-WR anti-correlation / tradeoff). The cmf 1.396 cited in 53/55 was the max among the strategies I tabulated from the register; I omitted ema200 because the register's trial-1 note gave no PF and I did not check the artifact. CONSEQUENCE: the council brief's 'uncleanable as written' premise is VOID. Entry-53's 'max 1.396 / nothing reaches 1.5' is corrected to: max PF overall = 1.556 (ema200, but a WR-fail); max PF among WR>=0.40 passers = 1.302 (mbv). The substantive points of entry 52 (amended gate never wired) and entry 55 (PF>=1.5 was an INTENTIONAL tightening; remediation is an operator-intent question) STILL STAND. The live question is now correctly the JOINT feasibility of WR>=0.40 AND PF>=1.5: across all 13 US strategies tested (full archetype spread), ZERO satisfy both; the two constraints are anti-correlated. By the identity PF = (WR/(1-WR)) * (avgWin/avgLoss), the joint corner WR>=0.40 & PF>=1.5 requires payoff ratio avgWin/avgLoss >= ~1.83 at WR 0.45 (>= 2.25 at WR 0.40) -- occupied by zero tested strategies (WR-passers have implied payoff 0.88-1.51; high-payoff strategies have WR 0.19-0.38). Not mathematically excluded, but empirically empty on this universe/mandate. NO verdict changed. Self-flagged and corrected per audit discipline (an error in the audit chain is itself logged and fixed in place).
- **rationale:** Audit honesty applies to my own entries as much as to the code: a wrong maximum (1.396) and a wrong 'uncleanable' conclusion are in the committed/pushed chain and must be corrected in place (a NEW append-only entry, not a silent edit) so the record stays trustworthy. The correction also sharpens the real question -- from 'PF>=1.5 is impossible' (false) to 'PF>=1.5 and WR>=0.40 are jointly unmet / anti-correlated' (true) -- which is what the operator must actually rule on.
- **alternatives_considered:**
  - Silently edit entries 53/55 (rejected -- the decision_log is append-only; corrections are new entries so the error AND its fix both remain visible)
  - Clarify only in conversation, leave the chain uncorrected (rejected -- the wrong claim is committed and pushed; an uncorrected audit chain is the exact failure the discipline exists to prevent)
  - Treat the error as minor (rejected -- it voided the council brief's premise and would have mis-framed the operator's A/B ruling)
- **audit_finding_refs:** `validation_us_ema200_1d_full.json provenance 2026-05-20T17:06:38Z: portfolio_expectancy(=PF) 1.5556, WR 0.1863, PORTFOLIO_FAIL on WR`, `corrects entries 53 + 55 ('max 1.396 / PF>=1.5 uncleanable')`, `true max PF among WR>=0.40 passers = mbv 1.302; identity PF=(WR/(1-WR))*(avgWin/avgLoss); joint corner needs payoff>=~1.83`, `the real finding is PF-vs-WR anti-correlation; entry 52 (gate unwired) + entry 55 (PF>=1.5 intentional) unaffected`
### Gate joint-calibration project: setup, gated, four locked principles

- **ts_utc:** `2026-06-16T06:41:37+00:00`
- **methodology_source:** `framework_directive`
- **decision:** OPERATOR-AGREED PLAN (resolves the gate-integrity thread, entries 52-56). The thread resolves into a GATE JOINT-CALIBRATION PROJECT: the validation gate's WR floor and PF floor must be calibrated JOINTLY (entry 56 showed WR>=0.40 and PF>=1.5 are anti-correlated and jointly unmet by all 13 tested strategies), and the documented amendments must be actually wired + enforcement-verified (entry 52). The project is GATED on BOTH (a) a benchmark study of proven funds + published literature AND (b) a healthy multi-model council, BEFORE any code/config/gate change. FOUR STANDING PRINCIPLES are LOCKED (also in operator memory) and govern all gate work: (1) STRICTER NEVER LOOSER -- any redesign must be stricter in aggregate; rules made smarter/more honest, never loosened to admit currently-failing candidates; reverse-engineering a threshold from what lets the portfolio pass is forbidden. (2) TEST-BREADTH NOT WINNER-FACTORY -- the engine may be upgraded to TEST more strategy types (e.g. cross-sectional dispatch, engine_roadmap.md), but horsepower never changes the Kelly bound (entry 39) or makes strategies win; edge comes from validation. (3) ENFORCEMENT-VERIFIED -- every gate rule verified as actually-enforced-in-code at every step. (4) BENCHMARK + COUNCIL BEFORE CHANGE. MEANWHILE the portfolio stays AS-IS -- nothing un-cleared, nothing admitted, no gate/config/code change. The WR-floor question (entry 46) and slot-3 candidate selection (Session A3) both FOLD INTO this project. SETUP THIS SESSION (no redesign): a benchmark-study research brief (read-only, council input) + confirmation of 3 genuinely-distinct healthy council seats. NO new gate designed or applied here.
- **rationale:** The integrity thread proved the documented gate was neither wired (52) nor jointly feasible as written (53/55/56). The honest resolution is neither 'enforce as-is' (which un-clears the live portfolio on an anti-correlated, possibly-miscalibrated criterion pair) nor 'loosen to admit' (forbidden) -- it is a deliberate JOINT recalibration grounded in external benchmarks and cross-checked by a real council, constrained to be net-stricter. Locking the four principles up front prevents the recalibration from drifting into a portfolio-rescue.
- **alternatives_considered:**
  - Enforce the documented gate as-is now (rejected -- un-clears the live portfolio on a jointly-infeasible/anti-correlated criterion pair before understanding whether the pair is sound)
  - Lower PF>=1.5 to admit the portfolio (rejected -- forbidden goalpost-gravity; violates principle 1)
  - Leave the gate as the legacy six and move on (rejected -- the entry-52 enforcement gap and entry-56 joint-calibration question are real and must be resolved, just not hastily)
- **audit_finding_refs:** `builds on + resolves entries 52 (gate unwired), 53 (expectancy==PF), 54 (degraded council), 55 (PF>=1.5 intentional), 56 (PF>=1.5 achievable; WRxPF anti-correlation)`, `four standing principles also in operator memory (gate-redesign-standing-principles)`, `WR-floor entry 46 + slot-3 Session A3 fold into this project`, `engine_roadmap.md (cross-sectional capability, gated) is the principle-2 test-breadth track`
### Council charge: migration test for the stricter gate

- **ts_utc:** `2026-06-16T06:56:51+00:00`
- **methodology_source:** `framework_directive`
- **decision:** Operator-set CHARGE for the gate-calibration council (benchmark study committed at aig/benchmark_study_gate_calibration.md). PRIMARY MIGRATION TEST the council must answer: under a frontier-aware, risk-adjusted, archetype-NEUTRAL gate that is STRICTER in aggregate (wire the currently-unwired amendments -- PF-CI>=1.0, OOS>=0.7xIS, 24-mo span, per-market trade floors -- AND raise the deflated-Sharpe floor + add Sortino/Calmar + PSR>0.95 + a tail / max-single-trade check), do the current live slots (Divergence slot 1, TRB-50 slot 2) still CLEAR? RULING: forward-only; and the OPERATOR ACCEPTS THE ANSWER EVEN IF IT UN-CLEARS A LIVE SLOT -- this is principle 1 in practice (stricter never looser; no portfolio-rescue). Two sub-findings the council must adjudicate: (a) the deflated-Sharpe haircut is scaled by 0.25 (haircut = 0.25*sqrt(2 ln N); aig/stats.py deflated_sharpe ~L50-56) -- too lenient vs the Harvey-Liu-Zhu t>3 multiple-testing literature? should the scaling rise? (b) PF>=1.5 is redundant under a DSR-centric gate (PF==expectancy, entry 53) -- keep it (frontier-form only) or drop it? The benchmark study's uniform finding: every rigorous vector points STRICTER (t>3, PSR, DSR, Sortino/Calmar, plus wiring the amendments), and flat WR floors are the anti-correlation-producing anti-pattern. This entry is the FORMAL CHARGE only; NO gate designed or changed; the council convenes ONLY on confirmed-clean, genuinely-distinct seats (seat verification in progress).
- **rationale:** The migration test operationalizes principle 1: a redesign is honest only if it is willing to un-clear the current portfolio. Recording the operator's pre-commitment to accept that outcome BEFORE the council runs removes the goalpost-gravity temptation from the redesign and binds it to be net-stricter. The two sub-findings are the concrete, decision-shaped questions the benchmark study surfaced.
- **alternatives_considered:**
  - Leave the council question implicit in the doc (rejected -- the operator's pre-commitment to accept un-clearing live slots is a principle-1 ruling that belongs in the binding decision_log, not only a doc)
  - Pre-judge sub-findings (a)/(b) here (rejected -- they are the council's to adjudicate; the charge only poses them)
  - Convene the council now (rejected -- gated on confirmed-clean distinct seats; setup not complete)
- **audit_finding_refs:** `aig/benchmark_study_gate_calibration.md (committed this milestone) -- the study + the council question`, `builds on entries 52-57; governed by gate-redesign-standing-principles (operator memory)`, `sub-finding (a): aig/stats.py deflated_sharpe haircut = 0.25*sqrt(2 ln N); sub-finding (b): entry 53 (PF==expectancy)`, `migration test targets live slots: divergence (trial 4), trb50 (trial 41)`
### Gate-calibration council: raise haircut, drop flat PF>=1.5

- **ts_utc:** `2026-06-16T08:59:11+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** GATE-CALIBRATION COUNCIL OUTCOME (one paid round on the entry-58 charge; council INFORMS only, no gate designed/changed). SEAT INTEGRITY: 3 genuinely-distinct families verified by returned model ID -- openai/gpt-5.1-20251113, google/gemini-2.5-pro, x-ai/grok-4.3-20260430 (no collapse/substitute). RESULT = 2 FULL (gpt-5.1, grok-4.3) + 1 PARTIAL (gemini truncated at the 1600-token cap; re-fetch returned 402 Payment Required, paid balance exhausted -- only gemini's Divergence line survived: 'depends on PSR>0.95'). UNANIMOUS DIRECTION (both full seats + gemini's partial direction): (1) RAISE the deflated-Sharpe haircut scaling from 0.25 toward ~0.6 (lenient vs Harvey-Liu-Zhu t>3; gpt 0.5-0.7, grok 0.6-0.8); (2) DROP the flat PF>=1.5 floor -- redundant under a DSR-centric gate since PF==expectancy; keep only as a frontier diagnostic, if at all. GENUINE-REASONING EVIDENCE (NOT echo, unlike the prior degraded round): independent numbers (haircut gpt 0.5-0.7 vs grok 0.6-0.8); DIVERGENT challenges (gpt-5.1 attacks archetype-neutrality as not universally binding; grok-4.3 attacks the migration test's FALSIFIABILITY -- 'aggregate stricter' lacks joint-distribution calibration); and an EMERGENT shared warning NOT in the brief -- 'stricter via many uncalibrated metrics' can itself become an OPAQUE, UNFALSIFIABLE, false-negative-prone gate = a NEW integrity failure. They reasoned to a caveat ON principle 1, not merely from it. KEY FINDING: the live slots Divergence (DSR 2.606) and TRB-50 (DSR 2.802) are strong on risk-adjusted return and LIKELY clear a stricter DSR-centric gate -- BUT their fate hinges on the NEW checks (PSR>0.95, Calmar/tail) which have NO thresholds yet, so the migration test is currently UNDER-SPECIFIED / unfalsifiable until concrete numbers exist. gpt-5.1's archetype-neutrality dissent is recorded as REASONED DISSENT, flagged not averaged. PROCESS LESSON: the abort-check verified model IDs but NOT finish_reason, so the gemini truncation slipped through; the next council must raise the token cap AND verify finish_reason=='stop' per seat before treating a reply as complete. No verdict changed; no code/config/gate touched.
- **rationale:** The council outcome must be recorded at its honest weight -- 2 full seats + 1 partial, budget-exhausted, NOT a clean 3-0 -- so it informs the redesign without inflation. The most valuable contribution is the emergent anti-thicket warning (stricter must be a SMALL well-calibrated metric set, not a hurdle pile) and the finding that the migration test cannot even be answered until concrete PSR/Calmar/tail thresholds exist. Logging the genuine-reasoning evidence distinguishes this from the prior echo round (entry 54).
- **alternatives_considered:**
  - Complete the gemini seat (rejected -- no top-up; direction already unanimous across the two full seats + gemini's partial; 2 full + partial suffices to INFORM)
  - Design the new gate now from this output (rejected -- council informs only; redesign is a separate operator-supervised session)
  - Record it as a clean 3-0 council (rejected -- gemini was truncated and unrecoverable; recorded as 2 full + 1 partial)
- **audit_finding_refs:** `seats: openai/gpt-5.1-20251113, google/gemini-2.5-pro (partial), x-ai/grok-4.3-20260430; entry-58 charge; brief 9ad054b + study a256ce8 as context`, `builds on entries 52-58; governed by gate-redesign-standing-principles (operator memory)`, `redesign = separate supervised session under anti-thicket discipline: smallest sufficient metric set; each NEW metric must prove INCREMENTAL value over the existing deflated Sharpe before adoption; calibrate jointly; re-run the migration test only with concrete thresholds`, `process fix for next council: raise token cap + verify finish_reason==stop per seat`
### Reproducibility gap: US market data was never snapshotted

- **ts_utc:** `2026-06-16T10:24:46+00:00`
- **methodology_source:** `audit_finding_resolution`
- **decision:** INTEGRITY FINDING (entry-52 class; logged on discovery, before the fix): the US historical verdicts -- including the LIVE slots Divergence (trial 4) and TRB-50 (trial 41) -- were validated against yfinance OHLCV fetched LIVE at run time and NEVER snapshotted. Evidence: data_cache/ holds 46 files, ALL UAE (.AD/.DU) + a vix cache -- ZERO US tickers; aig/data.py fetches US daily from yfinance with period='15y', auto_adjust=True, TODAY-anchored (L62-67), and cache-first applies ONLY to the UAE CSVs (L100-107). The register's 'reproducibility is the binding contract' (audit-linkage section) covered config_hash + code/file hashes but NOT the US market data, which yfinance revises continuously (auto-adjust splits/divs) and extends daily. Concretely: a re-run on 2026-06-16 returns ~27 more calendar days at the OOS tail + a trimmed 15y front + adjustment revisions vs the 2026-05-20/21/22 runs, so n changes, the 60/40 split shifts, and verdicts/DSR/WR/PF move. NET: the exact 2026-05 US inputs are unrecoverable and those verdicts are not byte-for-byte reproducible. This is a provenance gap, not mere logistics: verdicts the register treats as reproducible were validated on unpinned, drifting data. NO verdict is changed by this entry -- it RECORDS the gap. It is CLOSED GOING FORWARD by freezing a canonical yfinance snapshot into data_cache (next entry / step B), after which this capture, C's migration test, and all future US trials run against one fixed, regenerable dataset. (UAE was already pinned via data_cache, which is why UAE was unaffected; this gap was US/Crypto-only.)
- **rationale:** Reproducibility is the binding audit contract for this project, but it only ever covered code/config hashes -- the live US market data was never part of the hash, so US verdicts were never actually reproducible. That must be recorded as an integrity finding (entry-52 class) and fixed structurally (freeze the data), not waved off as a logistics quirk of the capture re-run. The fix (a frozen snapshot) matters more than the calibration it enables, because it makes ALL future US validation reproducible.
- **alternatives_considered:**
  - Treat it as logistics only (rejected -- claimed-reproducible verdicts were validated on unpinned drifting data; that is a provenance/integrity gap)
  - Pin the 2026-05 data retroactively (impossible -- the original US OHLCV was discarded; only UAE was cached)
  - Leave it unrecorded and just do a fresh capture (rejected -- audit discipline logs the gap on discovery; and a fresh capture must be paired with a forward freeze to actually close it)
- **audit_finding_refs:** `data_cache/ = 46 files, all UAE (.AD/.DU) + vix; zero US tickers`, `aig/data.py L62-67 (US daily = yfinance 15y, auto_adjust, today-anchored) + L100-107 (cache-first only for UAE CSVs)`, `register audit-linkage 'reproducibility is the binding contract' -- covered config/file hashes, not US market data`, `affected: ALL US (and Crypto) trials incl live slots divergence (trial 4) + trb50 (trial 41); UAE unaffected (already cached)`, `closed forward by the canonical-snapshot freeze (step B / next entry)`
